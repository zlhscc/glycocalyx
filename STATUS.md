# 專案現況

```
最後更新: 2026-08-09
最後 commit: 本批 schema commit，commit 訊息首行為 `schema: v11 — add claim_id and evidence format checks to §9`。
           §1 的數字所依據的 ledger commit 為 `ledger: backfill 6 claims in section 4.1.3 (pathway column)`
schema_version: 11
topics_version: 2
```

> **新 session 從這裡開始。** 讀完本檔，再依 §6 決定要不要讀其他檔案。

---

## 1. 帳本狀態

```
unverified  346
partial      30
unsupported  51
verified     22
contested     6
superseded    2
──────────────
總計        457
```

〔注意〕2026-08-13 `6.3` 第五批 8 列完成（進度 31/81）。「機制」欄無可排序的量，
改用另一形狀分批：**同一機制在不同藥物列反覆出現**——§5.5 十個藥中有八個
掛上「抑制 heparanase」，而那正是本檔的核心敘事。

**最重要的結果：SGLT2i 與 heparanase 的連結查無出處。**檢索回傳豐富的
SGLT2i 內皮文獻（MCP-1、endothelin-1、細胞老化、AMPK/KLF4 粒線體動態、
ROS、NF-κB/NLRP3 焦亡、VEGFR-2）但**無一提及 heparanase**。
〔重要〕**本檔 §7 整節建立在這個連結之上**，含六層機制表與「抑制強度 60-90%」等數值。
§7 抽取前應以不同措辭再驗一次；若確認查無，該節的抽取策略應改為
**先定性整節再逐條建列**，而非逐條建列後才發現整節無據。見批次報告 §7 第 3 項。

〔推論〕五列 `partial` 的共同結構為**機制鏈前段是標準藥理、後段接到 heparanase 時斷掉**
（PPAR-α→NF-κB 成立／→heparanase 查無；Nrf2→ROS 成立／→heparanase 查無）。
這解釋了為什麼十個藥有八個掛上同一機制——不是八個藥都抑制 heparanase，
而是每個治療策略都被接到本檔的核心終點上，讓整張表看起來連貫。

〔注意〕§5.5 同一張表出現**兩個「最強」**：第 1 列稱 SGLT2i 對 heparanase
表達抑制最強，第 2 列稱非抗凝修飾肝素為目前最強 heparanase 抑制劑。
兩者的軸不同（轉錄表達 vs 酵素活性 IC50 3 nM），表格未做區分。
此為 §3.20 的**表內版本**——不需跨章節，相鄰兩列就矛盾。

〔注意〕2026-08-13 `6.3` 第四批 7 列完成（進度 23/81），**首次以「表格欄位」為分批軸**。
依 §3.25 程序先排序「證據等級」欄——**排序未反向**，§3.25 不適用，改逐列查。
〔推論〕排序未反向本身是 §3.25 解讀的強佐證：同一張表，修復幅度欄反向而
證據等級欄合理，若兩欄用同一種方式構造則應彼此相關；方向不同代表來源不同。

逐列查揭露另一成因：**等級是為別的終點給的**（見 §3.24 補記的第二軸）。
GLP-1 一列取得**反駁記錄**：另有一項隨機分派雙盲安慰劑對照試驗
確實以 GlycoCheck 測量糖萼完整性，結果為兩組**無差異**；
而 LEADER／REWIND／STEP 的已發表子分析無一涉及糖萼。

§5.5 總表「修復幅度」與「證據等級」兩欄共 17 列至此全部判定，
該表尚有 21 列未判定。

〔注意〕2026-08-13 `6.3` 第三批 5 列完成（進度 16/81）。**§5.5「修復幅度」欄十列已全部判定，
全欄無一有出處。**本批依第二批規劃改變檢索目標——不找那個百分比，
而問「該藥是否有任何一手糖萼數據」——結果顯出區別：
DOAC（rivaroxaban）**確有**一手數據（PMID:39496251，HUVEC 中抑制 syndecan-1 脫落，
IQGAP1/PAR1-2/PI3K-Akt 途徑），其餘四項連一篇把該藥與糖萼連起來的研究都沒有。
若逕依抽樣結論一起判掉，這個區別會消失。

〔注意〕**本批更正前兩批 3 列違反 SCHEMA §7 的 `tier` 填寫**
（`6.3-sglt2-dkd-progression-40-50pct`、`6.3-sulodexide-thickness-40-70pct`、
`6.3-glp1-thickness-25-50pct`，evidence 僅有 `~` token 卻填了 tier）。
未被攔下的原因是驗收條件的排除子句剛好繞開要抓的對象，
已改為嚴格版並全表掃描（另 5 列混合 token 者確認合規）。
此為 §3.23「規格寫了、檢查沒驗」的變體：**檢查存在但條件寫錯**，
建議另立一條，見批次報告 §7 待裁示第 2 項。

〔注意〕2026-08-13 `6.3` 第二批 5 列完成（進度 11/81），全數 unsupported。
本批為**抽樣定性**而非逐列查證：抽 5 列跨越三個獨立文獻圈
（PBR 糖尿病、Sulodexide 糖尿病、重症醫學敗血症），
確認 §5.5「修復幅度」欄十列所用的「厚度恢復百分比」**計量本身不存在於文獻**。
各圈的實際計量分別為 PBR 微米、糖萼尺寸微米、syndecan-1 濃度 ng/mL；
對厚度改善的定性表述為「部分恢復」「恢復至基線厚度」。

〔推論〕**該欄排序與臨床證據強度呈反向**：實驗階段（全部前臨床）得 80-100% 為全欄最高，
非抗凝修飾肝素（停滯 Phase I）70-90% 次高，而有隨機分派試驗的 GLP-1、SGLT2i
僅 25-50%、30-60%。此與 §6.5 表「抑制強度」欄中和抗體（前臨床）得全表最高 `+++++`
**同型**。合理解讀為兩欄記錄的都是期望值或理論上限，卻被寫成實測值的形式。
是否新增為 §3 漂移模式，見批次報告 §7 待裁示第 3 項。

〔注意〕2026-08-13 `6.3` 第一批 6 列完成（進度 6/81），unsupported 5／partial 1。
**核心發現：人體糖萼研究的標準計量為 PBR（微米），與厚度成反比且非厚度本身，
文獻不做 PBR → 厚度恢復百分比的換算**——「厚度恢復 30-50%」這類表述
在來源文獻中沒有對應的量，不是數字錯而是單位不存在。
§5.5 治療總表「修復幅度」整欄 39 列皆為此型百分比，可能整欄同源，
建議下一批改以抽樣定性而非逐列查證，見批次報告 §7。

另查明兩個具名試驗：**DERBI 查無任何記載**；
**DELIGHT 真實存在但為白蛋白尿／CKD 試驗**（PMID:30992195，
主要終點為第 24 週 UACR 與 HbA1c），與視網膜終點無關。
EMPA-REG 等三項 CVOT **均未將神經病變列為終點**，但腎病變終點確實存在（降低 39%）。

〔注意〕2026-08-13 修訂單 `2026-08-13-source-heparanase-pipeline.md` **已裁示並執行**，
共 12 處改動：§6.5 與 §6.7 兩表各新增「查證狀態」欄（原欄位與數值一字不動）、
§6.6 與 §4.5 加標記、§3.5 加限定句、§5.5 修正代號重複計數。
全檔標記 13 個，已驗證與 ledger `status` 逐一對應。
待決取 3（§5.5「Phase II進行中」）依裁示不改，待 `6.3` 回填後併入下一份修訂單。

〔推論〕本次是專案首次觸發 `source-editing.md` §2 第 4 點的改寫門檻。
實作上發現該門檻與第 5 點（表格新增「查證狀態」欄）**並不衝突**：
新增欄本身就是「改寫」的一種形式，且比逐格加標記更能呈現「整表皆中」。
四列標記的表加欄後，未中的兩列寫「（待查證，屬節點 `6.3`）」，
整欄一眼可見哪些已查、哪些未查——這是散在儲存格裡做不到的。

〔注意〕2026-08-13 提出修訂單（原始提案內容），
彙整 `6.3.1` 與 `6.3.2` 兩節點查證結果對 `heparanase.md` 的六處修訂。
**§6.5 與 §6.7 兩張表各有四列需標記，觸發 `source-editing.md` §2 第 4 點的改寫門檻**，
不能以標記處理。另新發現第五處代號重複計數：§5.5 總表將 SST0001 與 Roneparstat
並列為兩個藥，實為同一化合物。

〔注意〕2026-08-13 `6.3.1` 第三批 8 列完成，**節點 100%（20 列）**，
本專案第三個完成的節點。為 `backfill.md` §3A 替換 3B（先判可判定性）的首次執行：
八列由**一次論域列舉**全部解決，非八次檢索。
論域列舉結果——曾進入臨床試驗的 heparanase 抑制劑**僅四個**
（Muparfostat、Roneparstat、Necuparanib、Pixatimod），多篇獨立綜述一致。
據此同時解決本檔四組長期衝突，並確認 §6.6「至少 20 個以上進入臨床試驗」
與事實差五倍、§6.5 抗體「Phase I-II」實為前臨床。

〔注意〕2026-08-13 `6.3.1` 第二批 5 列完成（進度 12/20）。其中 2 列由第一批
註冊基準直接判定、無新檢索——第一批以原文表格位置分組，漏掉散落於 §3.5、§4.5
的同化合物期別列。**註冊類節點應以受測對象而非原文位置分批**，已提請裁示。
兩項實質發現：(1) §3.5「兩者已進入一期」為真、§4.5「兩者已進入二期」一對一錯；
(2) Sulodexide 雖已核准，但核准適應症為血栓與慢性靜脈疾病，
原文所稱的糖尿病腎病變／微量白蛋白尿在主要市場屬「開發中」而非已核准。

〔注意〕2026-08-13 `6.3.1` 第一批 7 列完成（Pixatimod 與 Roneparstat 聚落），
為 `backfill.md` §3A 與 schema v12 `stale_after` 的首次實地執行，三檔間隔全部用到。
查證取得 Roneparstat 期別的註冊基準：`NCT01764880` 為其唯一人體試驗，
Phase 1、僅多發性骨髓瘤、2016-11 完成——**從未有 Phase II 存在**。
STATUS §3.20 所記的五個期別表述中，三個把它寫進了不存在的第二期。

〔注意〕2026-08-13 `6.3.2` 全節點回填完成（8 列，0 未判定），
為本專案**第二個完成率 100% 的節點**（首個為 `4.1.3`）。
verified 5／partial 1／unsupported 2。拆分提案 §2 的判斷獲證實：
該節點是其他列的前置依賴，其中一列的判定推翻了原文四處的藥物計數。

〔注意〕2026-08-13 節點 `6.3`（109 列）拆為 `6.3` 81／`6.3.1` 20／`6.3.2` 8。
只改 `section` 欄，`claim_id`、`statement`、`status`、`evidence` 全部不動，
故上表六項計數不變。同日 schema 升 v12，新增第 12 欄 `stale_after`。

〔注意〕123 → 455 為 2026-07-31 起九批抽 claim 的累計結果：
`axis` 全檔 16 條、`heparanase` §1 二十三條、§2 二十六條、§3 四十二條、§4 四十三條、
§5A 四十一條、§5B 四十六條、§6A 四十四條、§6B 五十一條，
另有 47 條走追加 `source_ref` 未建新列。抽取仍在進行中，
完成率會持續下降，這是預期的——**不要為了數字而略過任何一節**。

〔注意〕§3 的淨新增（42）高於 §2（26），行數卻相近。成因是 `heparanase.md`
為每個器官複製同一套段落結構，§2 有的數值 §3 幾乎都有一個對應版本，
依 SCHEMA §2「歸屬不同即為不同條」全部各自建列。§4 為 43，與 §3 同量級，符合預期。

〔注意〕總列數 121 → 123 是 2026-07-31 拆列的結果，不是新抽 claim。
`superseded` 首次出現：`5.4-s1p-preconditioning` 依 SCHEMA §6（v6）拆為兩列。
`superseded` 列不計入任何完成率——它既不是待辦也不是產出，是歷史紀錄。

〔注意〕追加 `source_ref` 的序列為 10、10、7、3、0（5A）、5（5B）。
5A 為 0 是因為 §5 是本檔唯一以平台為主題的一節；5B 回升但**幅度低於預測**——
預測「十項有六項已有列」用的是藥名為鍵，而總表收的是證據等級與修復幅度，
與前四節收的「某藥在某器官的試驗結果」量綱不同，依 SCHEMA §2 各自建列。
查重的鍵是數值加終點，不是藥名。§6A 為 3，集中在 §6.3.3 那張把前三節結論
濃縮成四列的表（四列中三列走追加）。

〔注意〕**節點 `7`（糖萼以外的肝素酶病理）自 2026-07-31 核准收錄後首次有住戶**，
§6A 一次進駐 19 列。`TOPICS.md` 對該節的保留（服務於措辭可靠度而非糖萼來源完整性）
獲得證實：19 列中 5 列是承載措辭或跨領域總括宣稱。

隨時可用此指令重新確認：

```powershell
Import-Csv ledger/claims.csv | Group-Object status | Select-Object Name, Count
```

```powershell
Import-Csv ledger/claims.csv | Where-Object priority -eq 'high' |
  Group-Object { $_.status -in 'verified','partial','unsupported','contested' } |
  Select-Object Name, Count
```

**依優先序的完成率**（這才是專案健康度指標；全體完成率會被大量 low/med 稀釋）：

```
high   已判定 32 / 268 = 12%
med    已判定 17 / 177 = 10%
low    已判定  1 /  10 = 10%

〔注意〕2026-08-07 的 `4.1.3` 回填是連續五批抽取後的第一輪回填，
`high` 完成率由 9% 回到 12%。八條全部落在 `contested`／`unsupported`——
`runbooks/backfill.md` §1 說「`unsupported` 是完全合格的產出，不是失敗」，
本批是該原則至今最極端的一次實例。

〔注意〕2026-08-09 批次全為 `med`（訊號途徑欄），`med` 由 6% 升至 10%，`high` 不動。
**這是第一批只動 `med` 的回填**，因為 `4.1.3` 的 `high` 已在前一批清空。
把一個節點做到 100% 必然會踩進 `med`——節點完成率與優先序完成率是兩個互相拉扯的目標，
本批選前者。
```

〔更正（2026-08-09）〕上一版記「high 32 / 272、med 10 / 172」。
以該版（commit `7615a3c`）的 `claims.csv` 重算，實為 **high 32 / 268、med 10 / 176**。
**四列 `med` 被算進了 `high` 的分母。** 兩者的百分比恰好不受影響（high 12%、med 6% 皆同），
所以這個偏差通過了當批的全部驗收。

〔推論〕與下方 2026-08-01 那次更正是同一族：**分子分母分開記，而只有百分比會被看**。
百分比對小幅度的分母錯誤不敏感，等於替原始數字提供了掩護。
本批起，此區塊的六個數字一律以下列指令重算後貼上，不從上一版沿用：

```powershell
$rows = Import-Csv ledger/claims.csv | Where-Object status -ne 'superseded'
'high','med','low' | ForEach-Object {
  $p = $_; $sub = $rows | Where-Object priority -eq $p
  $done = ($sub | Where-Object { $_.status -in 'verified','partial','unsupported','contested' }).Count
  "$p $done / $($sub.Count)"
}
```

〔更正（2026-08-01）〕上一版記「high 已判定 26 / 110」。以該版 `claims.csv` 重算，
high 之中 status 為 `verified`／`partial`／`unsupported`／`contested` 者共 **25** 條。
26 是把 `superseded` 的 `5.4-s1p-preconditioning` 算成了已判定，
與下方註記「`superseded` 不計入任何完成率」衝突。
本批起一律採註記所述的規則：**`superseded` 從分子與分母同時排除**。
兩種算法的百分比相同（皆 19%），差異只在原始數字——
所以這個偏差可以存活到下一批而不被任何驗收發現。
這一欄與識別碼計數同族：每批重算，不累加。

〔注意〕上一版此處記載的 NEEDS-REVIEW 例外（`5.4-s1p-preconditioning` 已檢索卻停在
`unverified`）已於 2026-07-31 拆列後消解。目前**沒有** `status=unverified` 而
`last_checked` 非空的列——這個異常狀態存在了一天，成因是程序缺口而非判定困難。

2026-07-30 改寫了六列的 `statement` 措辭（見 §5「已決」），**status 分布與總列數未變**。
改寫後 `statement` 欄的措辭掃描命中數為 0（原 6）。

有 `evidence` 的列共 42 條，去重識別碼 46 個（35 個 PMID、11 個 DOI）。
計數方式：去除 `!` 與 `~` 前綴後取唯一值（v6 後前綴有兩種，重算指令見下）。

**更正（2026-07-30）**：本行原記「19 條、25 個識別碼（21 PMID、4 DOI）」。
以上一個 commit 的 `claims.csv` 重算，當時實際為 19 條、**22 個**識別碼（18 PMID、4 DOI）——
列數正確，PMID 數多記了 3 個。成因不明，推測是手動累加而非重算。
日後一律以下列指令重算，不手動累加：

```powershell
$ids = Import-Csv ledger/claims.csv |
  ForEach-Object { $_.evidence -split ';' } |
  ForEach-Object { $_.Trim().TrimStart('!').TrimStart('~') } |
  Where-Object { $_ } | Sort-Object -Unique
$ids.Count
($ids | Where-Object { $_ -like 'PMID:*' }).Count
($ids | Where-Object { $_ -like 'DOI:*' }).Count
```

〔推論〕這一欄的用途是回答「帳本背後有多少獨立文獻」，是對外可引用的數字。
它與 §1 的 status 分布不同——status 分布每批都會被重新計算而自動校正，
識別碼數沒有任何機制會發現它偏了。凡不會自我校正的計數，都必須附上重算指令。
另有 3 條列的相關文獻只寫在 `note`，未進 `evidence`（見 §3.13、§3.16 與 §5 觀察中）。

`source_ref` 主要出處（第一個 anchor）：`glycocalyx/heparanase` 134 列、`glycocalyx/v3` 99 列、
`intake/2026-07-28-糖萼層生理` 24 列、`mitochondria/axis` 16 列。
另有 59 列同時標示兩份以上文件。
出現在任一 anchor 的總列數：`heparanase` 146、`v3` 99、`生理` 79、`axis` 20。

---

## 2. 已完成批次

| 日期 | 章節 | 條數 | 報告 |
|---|---|---|---|
| 2026-07-26 | 5.1（校準批次） | 3 | `reports/backfill/2026-07-26-5.1.md` |
| 2026-07-26 | 5.5 | 4 | `reports/backfill/2026-07-26-5.5.md` |
| 2026-07-27 | 2.1.1 / 2.2 厚度群 | 5 | `reports/backfill/2026-07-27-2.1.1-2.2.md` |
| 2026-07-28 | 生理.md 收錄（無檢索） | 53 追加 + 24 新建 | `reports/backfill/2026-07-28-intake-physiology.md` |
| 2026-07-28 | v3 §2.1.1／§2.2 標註（無檢索） | 4 處 | `reports/revisions/2026-07-27-source-2.1.1-thickness.md` |
| 2026-07-28 | 2.2 腦部與肺部血管床 | 5 | `reports/backfill/2026-07-28-2.2-brain-lung.md` |
| 2026-07-29 | 2.1.1 十一微米歸屬群 | 2 | `reports/backfill/2026-07-29-2.1.1.md` |
| 2026-07-29 | 2.2-B1 血管床型別機制 | 2 | `reports/backfill/2026-07-29-2.2-b1.md` |
| 2026-07-29 | 2.2-B2a 敗血症脫落時序 | 1 | `reports/backfill/2026-07-29-2.2-b2a.md` |
| 2026-07-29 | 2.2-B2b 動力學數字（半衰期／修復／降幅） | 3 | `reports/backfill/2026-07-29-2.2-b2b.md` |
| 2026-07-29 | v3 §2.2／§5.1／§5.5 標註與改寫（無檢索） | 13 處 | `reports/revisions/2026-07-29-source-2.2-5.1-5.5.md` |
| 2026-07-30 | ledger 六列 statement 措辭改寫（無檢索） | 6 列 | `reports/revisions/2026-07-30-rhetoric-policy.md` §6 |
| 2026-07-30 | 三個受追蹤檔措辭執行（無檢索） | 29 處 + 12 行 | `reports/revisions/2026-07-30-source-{v3,axis,heparanase}-wording.md` |
| 2026-07-30 | 5.3／5.5 PBR 群 | 2 | `reports/backfill/2026-07-30-5.3-5.5-pbr.md` |
| 2026-07-30 | therapeutic 群（跨 section） | 7 | `reports/backfill/2026-07-30-therapeutic.md` |
| 2026-07-31 | schema v6 執行（`~` 回填 4 列＋拆 `5.4`） | 4＋1 | `reports/revisions/2026-07-30-schema-*.md` §7 |
| 2026-07-31 | schema v7 執行（建 `TOPICS.md`＋`extraction.md`） | 0 | `reports/revisions/2026-07-31-schema-canonical-topics.md` §7 |
| 2026-07-31 | **抽取** `mitochondria/axis` 全檔 | 16＋4 | `reports/extraction/2026-07-31-axis.md` |
| 2026-07-31 | **抽取** `glycocalyx/heparanase` §1（總論） | 23＋10 | `reports/extraction/2026-07-31-heparanase-1.md` |
| 2026-07-31 | **抽取** `glycocalyx/heparanase` §2（DKD） | 26＋10 | `reports/extraction/2026-07-31-heparanase-2.md` |
| 2026-08-01 | **抽取** `glycocalyx/heparanase` §3（DR） | 42＋7 | `reports/extraction/2026-08-01-heparanase-3.md` |
| 2026-08-01 | schema v8 執行（`fact` 合法化＋18 列改派＋§9 值域檢查） | 18 列 | `reports/revisions/2026-08-01-claim-type-fact.md` |
| 2026-08-01 | **抽取** `glycocalyx/heparanase` §4（DN） | 43＋3 | `reports/extraction/2026-08-01-heparanase-4.md` |
| 2026-08-02 | schema v9＋topics v2 執行（人體閘門跨型別化＋8 列改派＋`5.6` 節點更名） | 8 列 | `reports/revisions/2026-08-02-schema-human-gate.md` |
| 2026-08-02 | **抽取** `glycocalyx/heparanase` §5A（早期診斷，L362-424） | 41＋0 | `reports/extraction/2026-08-02-heparanase-5a.md` |
| 2026-08-02 | 同一節兩次獨立抽取的比對（無檢索） | — | `reports/extraction/2026-08-02-heparanase-5a-crosscheck.md` |
| 2026-08-02 | **抽取** `glycocalyx/heparanase` §5B（治療總表，L426-461） | 46＋5 | `reports/extraction/2026-08-02-heparanase-5b.md` |
| 2026-08-02 | **抽取** `glycocalyx/heparanase` §6A（肝素酶專論，L463-531） | 44＋3 | `reports/extraction/2026-08-02-heparanase-6a.md` |
| 2026-08-02 | **抽取** `glycocalyx/heparanase` §6B（管線與上游調控，L532-592） | 51＋5，另修復 37 列 | `reports/extraction/2026-08-02-heparanase-6b.md` |
| 2026-08-07 | schema v10 執行（`source_ref` 格式檢查）＋ `runbooks/extraction.md` §6/§8 補條文 | — | `ledger/CHANGELOG.md` v10 |
| 2026-08-07 | **回填** `4.1.3`（肝素酶上游調控．誘導倍數欄） | 8 條：contested 1／unsupported 7 | `reports/backfill/2026-08-07-4.1.3.md` |
| 2026-08-09 | **回填** `4.1.3`（同表訊號途徑欄，含 1 次拆列） | 7 條：verified 1／partial 2／contested 2／unsupported 2，另附帶修訂 2 列 | `reports/backfill/2026-08-09-4.1.3.md` |

〔注意〕表中「條數」欄自 2026-07-31 起可能是抽取而非回填。
抽取批次的報告在 `reports/extraction/`，回填在 `reports/backfill/`。

〔注意〕2026-07-31 那一列不是回填批次，是 schema 變更的執行。
其中補跑了兩式檢索（`5.4-s1p-transplant` 的移植半句），詳見拆列提案 §7。

**`2026-07-26-5.1.md` 是判定標準的基準範本。** 任何新加入的執行者（人或 agent）
應先讀它，再開始工作——它示範了判定的細膩度、note 的寫法，
以及「找到答案但取不到識別碼時該怎麼做」。

---

## 3. 已確認的系統性問題

這些是跨批次浮現的模式，不是個別錯誤。處理後續章節時應預期它們重複出現。

### 3.1 跨疾病數值拼接

`source/glycocalyx/v3.md` 第 5.1 節將三個數字並列呈現，讀來像同一項研究的完整表現，
實際上分別來自：2025 年敗血症薈萃分析（OR 2.04，真）、創傷內皮病變文獻（40 ng/mL，
真但錯置）、來源不明（85%/78%）。

**通則**：凡「cutoff + 敏感度/特異度 + 效應量」三者並列的主張，預設不同源，分開查證。

### 3.2 敏感度／特異度整欄查無出處

第 5.5 節生物標記表的四組敏感度／特異度（syndecan-1、endocan、HS 片段、低分子量 HA）
全部查無出處，而同一張表的效應量（OR 2.04、OR 5.06）多半可驗證。

**通則**：看到「敏感度 X% / 特異度 Y%」而未同時給出 cutoff 與終點定義的主張，
預設有問題。真實文獻報告 ROC 結果時幾乎必然同時給出切點。

**已知陷阱**：endocan 宣稱 77%/70%，而重症肺炎預測 ARDS 的真實數據是 78.7%/70.3%
（cutoff 11.6 ng/mL）。只比對數字會誤判為 verified，必須連同疾病與終點一起核對。

### 3.3 逆向指標被寫反

2.1.1 節宣稱「臨床與活體共識厚度 2-4 μm」，查無出處。臨床實測厚度約 0.5 μm，
而 GlycoCheck 的 PBR 值恰好落在 1.8-3.0 μm——PBR 是紅血球穿透深度，**升高代表糖萼變薄**，
方向與厚度相反，且原文 2.1.2 節自己寫對了。

**通則**：凡「數值越高代表功能越差」的逆向指標（PBR、阻力指數等），
在轉述中被當成正向量的機率很高。文件中出現這類指標時，回頭核對方向。

### 3.4 「共識」宣稱是無出處的徵兆

`2.1.1-consensus-2-4um` 與 5.5 節的敏感度/特異度同型：宣稱有共識、但無任何文獻可追。
真的有共識時，文獻會引用具體綜述或指引。

**2026-07-28 擴充**：這個模式不只出現在數值上。`source/glycocalyx/heparanase.md` 全文：

```
共識                        12 次
國際共識                     7 次
指引 / guideline             0 次
ADA / EASD / KDIGO / ESC / ISPAD   0 次
```

宣稱國際共識七次，未指名任何學會或立場聲明。

**通則**：權威性宣稱（「國際共識」「新典範」「臨床標準」）與數值宣稱同樣可證偽，
但更難察覺——數值錯了看得出來，「國際共識」錯了看不出來，而它承載的說服力更強。
處理方式見 SCHEMA §6：把宣稱本身建成 claim，改寫為可指名的形式，查不到即 `unsupported`。

**附帶**：時間形容詞（「2025 年 11 月最新」）進了 statement 之後，
會隨時間自動變成偽陳述，而沒有任何機制會提醒你去改。一律移除，時間入 `note`。

### 3.5 自我引用式「更新」

新增於 2026-07-28（來源：`reports/revisions/2026-07-28-intake-physiology-md.md`）。

`糖萼層_Glycocalyx_生理.md` 的「表2：糖萼層生物標記臨床更新（基於2025年薈萃分析）」
與同一份文件前面的「表1」**六列逐字相同**，內文卻寫「endocan 新增 77%/70%；ecSOD 活性 68%/82%」
——這兩組數字在表1裡本來就有。所謂更新沒有更新任何數值。

**通則**：標題宣稱「更新版」「基於最新薈萃分析」的表格，先與被它取代的表格逐格比對。
比 §3.4 更隱蔽，因為標題本身就在提供權威性。

### 3.6 歸屬與數值在轉述間漂移

新增於 2026-07-28（同上）。

同一個 11 μm，`v3` 歸給「腎絲球微血管」、生理.md 一處歸給「腦部與腎臟」、另一處寫「腎絲球 4-11 μm」，
而實查出處（`PMID:21474821`）的樣本是體外培養的牛主動脈內皮細胞。
同理，syndecan-1 預測死亡的敏感度在生理.md 正文寫 90%、同檔表格寫 85%。
再者，同一份文件對腦微血管厚度給出 1-3 μm（正文）與 0.54/0.23 μm（檢視段，小鼠）
兩個相差一個數量級的值，且未察覺衝突。

**通則**：同一主張在不同文件（或同一文件不同位置）出現時，先比對數值與歸屬是否一致。
**漂移本身就是「無原始出處」的指紋**——有出處的數字不會在轉述中換血管床或換百分比。

---

### 3.7 LLM 產出文件是多次問答的串接

新增於 2026-07-28（來源：`reports/revisions/2026-07-28-intake-heparanase-mito.md`）。

`glycocalyx_Heparanase.md` 有 7 個各自從「一、」開始的章節樹，
`The_Mitochondria-Glycocalyx_Axis.md` 有 3 個。表面看是一份文件，實際是多次獨立問答的串接。

**通則**：收錄任何 LLM 產出的長文件前，先數「一、」或 `##` 層級的重複次數。
節號碰撞會讓 `claim_id` 與 `source_ref` 無法唯一定位，必須先重編。
另檢查 Google Docs 匯出造成的跳脫 markdown（`\#\#\#`、`\-`），否則章節解析不出來。

---

### 3.8 搬移檔案會留下過期路徑引用

新增於 2026-07-28。

`source/glycocalyx_v3.md` 改為 `source/glycocalyx/v3.md` 之後，全庫殘留 20 處舊路徑引用。

**通則**：搬移或改名 `source/`、`ledger/`、`runbooks/` 下的任何檔案後，
立即全庫掃描並分三類處理：

```powershell
Get-ChildItem -Recurse -Include *.md,*.csv | Select-String "舊路徑"
```

| 類別 | 處置 |
|---|---|
| 活文件（`CLAUDE.md`、`STATUS.md`、`runbooks/`、`ledger/SCHEMA.md`） | **必改**，agent 會照著執行 |
| 待核准提案（`reports/revisions/` 未核准者） | **必改**，核准後會照著執行 |
| 歷史紀錄（`reports/backfill/`、`ledger/CHANGELOG.md`、已核准提案） | **不改**，改了就失真 |

〔推論〕第三類是關鍵。批次報告記錄的是當時的檔案狀態，
把它改成今天的路徑等於竄改紀錄，日後無法還原判定當下看到的是哪份文件。

---

### 3.9 定性描述被換算成具體數值

新增於 2026-07-28（來源：`reports/backfill/2026-07-28-2.2-brain-lung.md`）。

`v3` §2.2 稱腦微血管糖萼「1-3 μm」。文獻確實說腦部糖萼**比心、肺更厚更密**
（Ando 2018：腦內腔覆蓋率 40.1% vs 心 15.1% vs 肺 3.7%），
但兩項獨立的小鼠硝酸鑭 TEM 直接測量都是次微米級（0.301 μm、0.540 μm）。

**通則**：這與 §3.3「逆向指標被寫反」不同型——那是方向錯，這是**方向對但被賦予了
一個原文沒有的具體數值**。比方向錯更難察覺，因為敘述讀起來完全合理。
凡「某部位比別處厚／密」的比較性描述後面跟著一個絕對數值，該數值須獨立查證。

---

### 3.10 通則範圍被接到論文的研究對象上

新增於 2026-07-29（來源：`reports/backfill/2026-07-29-2.1.1.md`）。

`intake/2026-07-28-糖萼層生理` 稱「腦部與腎臟等微血管中可達 11 μm」。
追溯後，唯一同時提到 11 μm 與這兩個器官的文獻是 `DOI:10.1371/journal.pone.0161610`：
一篇研究人腦內皮與人腎絲球內皮**奈米粒子攝取**的論文，
在引言處以通則語氣引用了與器官無關的「0.5-11 μm」，所引四篇無一為腦或腎，
且該文自己沒有量測任何厚度。

**通則**：這與 §3.6「歸屬漂移」是同一族但機制更具體——
漂移不必經過多次轉述，**一篇論文就足夠**：
研究對象寫在標題與摘要，通則數值寫在引言，兩者在同一頁上，接起來就成立。
凡遇到極端值被安在特定器官上而查無實測，先找這種論文（檢索式見 `queries.md` §1.7）。

〔推論〕這也解釋了為什麼這類錯誤特別難用「查證數值」的方式抓出來：
數值是真的，論文是真的，論文確實研究該器官——只有「數值屬於該器官」這個連接是假的，
而那個連接不存在於任何一份文獻裡，是轉述時新生成的。

---

### 3.11 一列多宣稱時，tier 必然誤導

新增於 2026-07-29（來源：`reports/backfill/2026-07-29-2.2-b1.md`）。

`2.2-glomerular-hs-charge` 的 statement 含三個可獨立證偽的宣稱，判定結果各不相同：
「極厚」被反駁、「富含 HS」未見反駁、「電荷選擇性由 HS 承擔」被同一篇支持文獻的
對照組推翻。三者共用一個 `tier` 欄。

該列 `tier=animal`，但那個 animal token 只支持結構覆蓋，不涉及電荷機制；
電荷機制唯一的證據是 in_vitro，而且是反面的。

**通則**：SCHEMA §5 已處理「反駁 token 不得抬高 tier」（v2），
但沒有處理**同一列內不同子宣稱由不同 token 支持**。`tier` 是欄位層的單一值，
在混合宣稱列上必然指向其中一個子宣稱，讀者無從得知是哪一個。

〔推論〕這不是新增規則能解決的，正解是不讓這種列存在——
嚴格執行 SCHEMA §6「一列一個主張」。回填時若發現既有列是混合宣稱，
先提拆列，不要先判定。本批因判定已完成才發現，故仍寫入判定，
但拆列提案列為待決（見 §5）。

### 3.12 歸屬漂移也發生在分子層

新增於 2026-07-29（同上）。

§3.6 記錄的漂移是**器官**層（11 μm 從培養細胞漂到腦與腎）。
本批發現同型錯誤發生在**分子**層：v3 §2.2 把腎絲球的電荷選擇性歸給 heparan sulfate，
而 `PMID:17942961` 的酵素對照顯示切除 HS 對跨內皮電阻無影響，
承擔電荷的主要是唾液酸。

**通則**：「某結構藉某分子達成某功能」的三段式宣稱，中間那個分子最容易被替換成
該領域最有名的那一個。查證時把功能與分子拆開查，檢索式見 `queries.md` §1.8。

**2026-07-29 B2b 擴充：被吸走的不只是功能，也可以是數值。**

`v3` §2.2 稱「Syndecan-1 半衰期約 2-8 小時」。查無任何 syndecan-1 的半衰期實測。
2-8 小時這個量級屬於**細胞表面 HSPG 這個分子類別**的周轉：
大鼠卵巢顆粒細胞 GPI 錨定型內吞 t½ 約 3 h、蛋白嵌入型約 4 h（`PMID:1533636`）；
大鼠肝細胞完整 HSPG 表面滯留 T½ 約 2-3 h（`PMID:11755208`）。皆非內皮細胞。

也就是說，**類別層級的量測值被寫成該類別中最有名成員的屬性**。
機制與本節原本記錄的功能歸屬漂移相同，只是被搬移的東西從「功能」換成「數值」。

〔推論〕syndecan-1 在本領域的高知名度來自它是**可抽血定量的血漿生物標記**，
而不是來自它的周轉動力學被研究得特別透徹。知名度與研究深度不一致的分子，
最容易吸走整個類別的屬性。檢索式見 `queries.md` §1.10。

〔推論〕另有一個未解的歧義：「血漿可溶性 syndecan-1 的清除半衰期」與
「細胞表面 syndecan-1 核心蛋白的周轉半衰期」是兩個不同的量，
原 statement 未指明是哪一個，而文獻裡兩者都沒有 2-8 小時的實測。

---

### 3.13 兩個相反的過程共用同一個數字

新增於 2026-07-29（來源：`reports/backfill/2026-07-29-2.2-b2a.md`）。

`v3` §2.2 稱「敗血症中糖萼可於 30 分鐘內大量脫落」。
敗血症模型的實測時序是小時級（`PMID:29058634`：12 h 7.8 ng/ml、24 h 14.4 ng/ml）；
分鐘級脫落的實測全部出自缺血再灌流（`PMID:17923576`：再灌流 0-15 分鐘內升 42 倍）。

但「30 分鐘」在**敗血症**文獻裡確實存在——它指的是治療性**修復**的時間：
LPS 小鼠經靜脈輸注預組裝糖萼的脂質體奈米載體後，糖萼於 30 分鐘內恢復。

**通則**：這與 §3.3「逆向指標被寫反」不同型。§3.3 是同一個量測值的方向讀反；
本例是**兩個相反的過程共用同一個數字**，轉述時取了錯的那一個。

〔推論〕這是目前記錄的所有型態裡最難偵測的一種。
數字是真的、疾病是真的、時間尺度是真的、文獻是真的——**只有動詞是假的**。
數值查核完全抓不到它，因為要查核的東西不在數值裡。
凡遇到「某狀態下 X 於 T 時間內發生」，把 X 的反向過程也查一次（檢索式見 `queries.md` §1.9）。

---

### 3.14 範圍被整段往上平移

新增於 2026-07-29（來源：`reports/backfill/2026-07-29-2.2-b2b.md`）。

`v3` §2.2 稱「修復時間：需要 7-14 天才能完全重建」。
實測的內源性完全回復是 **5-7 天**（小鼠提睪肌小靜脈，μ-PIV，`PMID:19443840`）。

7 是實測範圍的**上界**，卻成了 statement 範圍的**下界**；14 在任何一篇裡都不存在。

**通則**：這與 §3.9「定性描述被換算成具體數值」不同型——那是原文根本沒有數值，
本例的數值存在且被正確引用了一半。也與 §3.13 不同型——過程沒有搞錯，就是同一個過程。
錯的是**範圍的位置**：下界取自原文上界，上界則是憑空外推。

〔推論〕這型的檢出難度落在 §3.9 與 §3.13 之間。
數值查核抓得到（14 天查無出處），但只要核對的方式是「statement 的數字有沒有出現在文獻裡」，
7 就會命中，整條就會被判為相符。**核對範圍時要比對兩端，不是比對是否有交集。**

---

### 3.15 量綱不同的兩個指標被合併成一個範圍

新增於 2026-07-29（同上）。

`v3` §2.2 稱「病理狀態下：可減少 50-90%」，語境上明確指厚度。
實測的厚度降幅集中在 40-50%（多物種、多疾病、多量測法，見 `queries.md` §2.1.2），
沒有任何一筆接近 90%。

90% 那一端出現在**同一篇論文的另一個量測**上：`PMID:16567538` 以雙示蹤劑稀釋法
測得的全身糖萼**體積**，由 1.5±0.1 L 降至 0.2±0.1 L，降幅約 87%。
同篇的舌下微血管**厚度**降幅是約 44%。一篇論文同時提供了 50-90% 的兩端。

**通則**：凡「減少 X-Y%」形式的主張，先確認 X 與 Y 是否來自同一種量測。
體積正比於厚度的高次方，兩者的降幅在數學上本來就不可能相等，合併成一個範圍不成立。
本領域四種指標並存且常在同一篇論文中同時出現：
厚度（μm）、體積（L）、覆蓋率（%）、PBR（μm，逆向）。

這與 §3.3「逆向指標被寫反」同族但不同型：§3.3 是方向讀反，本例是**量綱被抹平**。

〔推論〕還有一個放大因子：**一份文件裡最常出現的數值範圍，會成為其他數字的吸引子。**
`50-90%` 在本語料庫出現四次，其中三次是不同的量——
HS 佔總 GAG 的組成比例（`v3` L128、`heparanase` L21、`生理` L28/L154）、
病理厚度降幅（`v3` L103）、動物實驗中 HS 損失比例（`heparanase` L44）；
第四次是同一種量的不同數值，糖尿病患者厚度降幅 50-70%（`heparanase` L62）。
凡在同一份文件內重複出現的數值範圍，逐處確認它們是不是同一個量。

---

### 3.16 譯名相近的相鄰概念被互換

新增於 2026-07-30（來源：`reports/backfill/2026-07-30-5.3-5.5-pbr.md`）。

`v3` §5.3 稱「糖萼生物標記異常先於認知功能衰退 2-3 年出現」。四次檢索查無任何
以糖萼指標為曝險、以認知衰退為終點的縱向研究。同量級的數字存在於 `PMID:39494362`，
但那是**腦脊髓液的 N-醣鏈結構**（bisecting GlcNAc）：

```
glycan biomarker      醣類生物標記
glycocalyx biomarker  糖萼生物標記
```

英文兩詞共用詞根，中文的「醣／糖」更是同音同源。糖生物學與血管生物學各有一套
「醣」的研究，標記完全不同，但在中文轉述裡只差一個詞。

**通則**：這與 §3.12「歸屬漂移發生在分子層」同族但更外層——
§3.12 是同一領域內把功能或數值歸給錯的分子，本例是**跨到相鄰領域**。

〔推論〕這一型的檢出成本特別高：檢索式裡放 `glycocalyx` **永遠查不到**那篇論文，
它根本不在那個檢索空間裡。必須先懷疑「這個詞是不是被換過」，才會去查另一個詞。
操作方式見 `queries.md` §7.2——三次落空後把詞換掉再查一次，成本一次檢索。

〔推論〕同一條 statement 上還疊了第二層錯誤：`PMID:39494362` 的 β = 2.53 ± 0.85 年
是 MMSE **軌跡的整體提前量**，不是「標記異常領先終點幾年」的**前導時間**。
即使概念沒被換掉，那個數字也不能直接搬。帶 `± SE` 的年數幾乎一定是迴歸係數
而非觀察到的間隔，見 `queries.md` §7.1。

---

### 3.17 介入被換掉，其餘要素全部保留

新增於 2026-07-30（來源：`reports/backfill/2026-07-30-therapeutic.md`）。

`v3` §4.2.3 稱「2025 年的小型隨機試驗顯示，荷爾蒙替代療法與糖萼厚度的改善相關」。
三式檢索查無任何以 HRT 為介入、糖萼厚度為終點的人體研究。
但存在一篇六項要素中五項全中的論文（`DOI:10.14814/phy2.70428`，`NCT:NCT06071728`）：

```
2025 年        ✓    停經後女性        ✓
小型 n=22      ✓    糖萼厚度改善      ✓
隨機試驗       ✓    荷爾蒙替代療法    ✗ ← 實為糖萼標靶補充劑
```

而且該試驗的排除條件明文包含「近 6 個月內使用或曾使用荷爾蒙治療」——
受試者恰好是**排除了 HRT 的一群人**。

**通則**：這與 §3.13「兩個相反的過程共用同一個數字」同族——那裡假的是動詞，
本例假的是**主詞（介入）**。檢出難度相近，因為錯的東西同樣不在數值裡：
年份、規模、設計、族群、終點、效果方向全部可查證且全部為真。

〔推論〕本型的反查手法是「把可疑的那一項拿掉」：查詢裡不放介入名，
只放年份＋族群＋終點＋量測法，讓其餘要素去定位原文。見 `queries.md` §8.4。
判準是**具體要素越多、合起來卻查不到，越可疑**——一個憑空捏造的宣稱不會同時
把年份、樣本數、研究設計、族群與量測法都寫對。

### 3.18 抽 claim 時模態被強化

新增於 2026-07-30（同上）。前十七項記錄的都是原文的問題，本項是**本專案自己的**。

`v3` §2.4 原句是「單純補充 GAG 前體**未必能**恢復功能」，
抽進 `2.4-precursor-insufficient` 時寫成「**無法**恢復糖萼功能」。
或然變全稱，模態被強化了一級。

這個差別不是修辭問題。查證結果顯示：人體 n=22 隨機試驗中前體補充無效
（`DOI:10.1152/japplphysiol.00651.2024`），但同配方在 db/db 小鼠有效，
且舒洛地特在第二型糖尿病可部分恢復糖萼（`PMID:20865240`）。
「未必能」與這組證據相容，「無法」不相容。**強化過的 statement 會被自己的證據推翻。**

**通則**：抽 claim 時逐條核對模態詞。原文的
「未必／可能／傾向於／在部分情況下」不得省略或換成全稱。
這與 CLAUDE.md 第 4 條「數值必須與原文逐字相符」是同一條規則的非數值版本——
模態詞承載的是主張的**範圍**，範圍被放大等同於數值被改寫。

〔推論〕這一型在三檔抽 claim（§5「待處理」，約 54 行）之前必須先記下來，
因為那是本專案至今最大的一次抽取，而抽取正是這型錯誤發生的環節。
既有的 121 列裡可能還有同型未發現者——凡 statement 讀起來是斷然全稱、
而原文段落語氣是推測性的列，都值得回頭比對。

---

### 3.19 節號被隨手指派，直到第二份文件進來才顯現

新增於 2026-07-31（來源：`reports/revisions/2026-07-31-schema-canonical-topics.md`）。

`heparanase.md` 收錄時做過章節重編（42 節 → 1–7.4），`axis.md` 也重編過。
兩次都只考慮文件內部結構，未對照 `v3` 已佔用的座標。
結果是三份文件的 `section` 座標系全面碰撞：重疊 23 個節號，
其中 19 個已被既有 121 列佔用且語意不相干
（`v3 §4.1` 是酶介導降解，`heparanase §4.1` 是糖尿病神經病變的時間序）。

壞掉的不是 `claim_id`（slug 不同不會撞），是 `section` 欄作為主題座標的功能，
亦即 §4 批次劃分與跨文件去重所依賴的那一項。

**通則**：這一型的特徵是**在第二份資料進來之前完全不可見**。
重編當下三份文件沒有任何共用的 claim，碰撞不會顯現；
要等到 121 列都寫完、第二份文件要抽取時才爆出來。

〔推論〕與 §3.6 的漂移偵測依賴同一條鏈：兩者都要有第二個樣本才能發現。
差別在漂移是**內容**的問題、本項是**座標**的問題，而座標壞掉會使漂移偵測本身失效——
同主題的列不聚在一起，就沒有東西可以互相比對。

已由 schema v7 修正：`section` 改取 `ledger/TOPICS.md` 的正規主題樹節號，
來源文件的章節重編號不再影響 `section`。
成因寫進 `runbooks/extraction.md` §8 作為第四個已知失效模式——
**提案會被歸檔，runbook 每次抽取都會讀**。

---

### 3.20 平行章節之間的內部矛盾，只有並排才看得見

新增於 2026-08-01（來源：`reports/extraction/2026-08-01-heparanase-3.md` §4）。

`heparanase.md` 為每個器官複製同一套段落結構（時間序／機制／核心病理／
臨床證據／藥物／結論）。抽 §3（DR）時發現三處**同一份文件內部**的不一致：

1. **比較級與時間序方向相反**。`3.2` 標題稱 DR 的糖萼損傷「比腎臟更早、更劇烈」，
   但 `3.1` 的第一階段是「數週內」，`2.1` 的 DKD 版本是「數天～數週」——DR 反而較晚。
2. **同一節內定性與定量不相容**。`3.2` 稱視網膜毛細血管「幾乎完全依賴糖萼維持 BRB」，
   相距 20 行的 `3.3` 稱糖萼提供「60-80%」的電荷與機械屏障。
3. **同一個分子在同一節內同時是上游與下游**。`3.2` 項 4 的 VEGF-A 誘導 heparanase，
   `3.3` 項 4 的 VEGF 是缺氧經 HIF-1α 的產物。原文以「局部惡性循環」串接，
   但兩個方向各自需要不同的證據。

**通則**：這三處都不是數值錯誤，而是「各自看合理、並排才矛盾」。
與 §3.19（節號碰撞）同型——**在第二個平行樣本出現之前完全不可見**；
差別在 §3.19 要等第二份**文件**，本項只要同一份文件的第二個**器官章節**。

〔推論〕可見的時機只有抽取階段。回填時這些條目會落在不同批次
（時間序、屏障百分比、VEGF 通路各自歸屬不同的查證群），
屆時沒有任何一批會同時看到矛盾的兩端。
**抽取平行章節時必須把對應句並排，這是唯一的時機。**

〔推論〕`3.2` 那個「比腎臟更早」的比較級還有第二層意義：
它是**論述強度的通貨膨脹**。同一份文件對三個器官各寫一節，
每一節都要說明自己這個器官特別重要，於是逐節出現「比前一節更嚴重」的措辭，
而底下的數值並未跟著移動。

**2026-08-01 §4 抽完後確認為遞增序列**，且不只出現在標題：

| 宣稱 | DKD | DR | DN |
|---|---|---|---|
| 可逆性 | 「最早的**可逆階段**」 | 「**可逆階段**的關鍵」 | 「早期干預可**完全逆轉**」 |
| 對糖萼的依賴 | —— | 「**幾乎完全**依賴」 | 「**完全**依賴」 |
| 節標題比較 | 「比一般慢性發炎更嚴重」 | 「比腎臟更早、更劇烈」 | 「比腎臟、視網膜**更敏感**」 |

而時間序數值方向相反：DKD 第一階段「數天至數週」，DR 與 DN 皆「數週內」——
**被宣稱最早的兩個器官，時窗反而比 DKD 晚或相同**。

〔推論〕本序列有一個實用的副產品：**模態最強的那一條通常最沒有出處**。
`5.6.3-dn-preclinical-fully-reversible`（可完全逆轉）是三條裡唯一的絕對宣稱，
而三條都未附任何逆轉實證。查證時從最強的那一條開始——
若它站不住，較弱的兩條也要一併收斂。

〔注意〕§4 另發現本族最硬的一個實例：**同一個可查事實在同一份文件裡有兩個值**。
`§3.5` 稱 heparanase 抑制劑「進入人體一期」，`§4.5` 稱同樣兩個化合物「進入人體二期」，
兩處相距約 80 行。兩條各自建列並互相標註
（`6.3-heparanase-inhibitor-dr-phase1`、`6.3-heparanase-inhibitor-phase2`）。

〔注意〕（5B，2026-08-02）本族第三與第四個實例，都出自治療總表：

1. **試驗期別出現第三處**：總表第 4 列稱非抗凝修飾肝素「Phase II 進行中」。
   同一類化合物，兩個值，三處出現。§6.7 的管線表會第四次觸及，屆時可定案。
2. **結論與自身表格不一致**，且這是本族至今**最乾淨的一個**——
   結論稱三聯療法是「已知最強的糖萼修復組合」，而同文件表格中修復幅度最高的
   是非抗凝修飾肝素（70–90%）與實驗階段策略（80–100%）；
   結論的「6–12 個月恢復 40–70%」中，40–70% 在表格裡是 Sulodexide **單方**的值。
   前兩個實例需要判斷才看得出矛盾，本例只需把結論的數字與表格的數字對一下。

〔注意〕（6B，2026-08-02）試驗期別集滿**五處**，且方向可以定案了。
五處為：§3.5「已進入人體第一期」（DR 段落內）、§4.5「已進入人體第二期」（DN 段落內）、
§5.5 總表「Phase II 進行中」、§6.5 表「Phase II（胰腺癌、DKD）」、
管線總表「Phase I（完成）Phase II（中止或未繼續）」。

〔推論〕**管線總表的可信度高於前四處，而且它給出了解釋。**
它具名試驗編號（NCT01764880）、具名開發公司、並記錄停滯原因（腫瘤療效有限）；
前四處只給期別，無其他可核對的細節。更關鍵的是管線前言直接說明了前四處為何會錯——
「糖尿病腎/視網膜/神經病變領域僅有動物/前臨床數據，尚未有正式人體試驗」。
Roneparstat 與 PG545 確實進了人體試驗，**但那是腫瘤適應症**；
§3.5 與 §4.5 把腫瘤試驗的期別寫進了糖尿病併發症的段落。

這是**數值被跨疾病脈絡挪用**的實例，只是挪的不是數值而是試驗階段——
同一個事實在正確的脈絡下為真，搬到另一個脈絡就成了假。
定案留給回填（本批不做檢索，上述為推論而非判定）。

〔注意〕（6A，2026-08-02）試驗期別出現**第四個值**：§6 的臨床試驗管線總表
把 Roneparstat 列為「Phase I（完成）Phase II（中止或未繼續）」。
四處為 §3.5「一期」、§4.5「二期」、§5.5 總表「Phase II 進行中」、
管線總表「Phase II 中止或未繼續」。**最後一個與前三個在方向上相反**
（前三處都指向進行中或已達成，管線表指向中止）。
該表在 6B 範圍內，屆時四處並排定案。

〔推論〕**表格給的最保守、結論給的最寬**——5A §3.2 提出的這條預判在 5B 命中第二次。

〔注意〕（6B，2026-08-02）**該預判出現第一個反例。** Sulodexide 的機制在本檔三處
描述不同：§5.5 總表「強力抑制heparanase（IC50極低）」、管線總表「部分抑制heparanase」、
§6.7「主要機制為補充GAGs而非純粹抑制heparanase」。最寬的是**表格**，最保守的是文字段落。

〔推論〕修正後的說法：**離該主題最近的那一處通常最保守**。
§6 是 heparanase 專論，Sulodexide 在此是被審視的對象；
§5.5 的治療總表裡它是被推薦的方案之一。**立場決定強度，位置只是相關而非成因。**
兩個樣本都指向同一個成因：同一件事在前言、表格、結論三處的修辭功能不同，
而數值跟著修辭功能放大或收縮。查證時應以表格值為準、以結論值為待驗證的放大版本。

---

### 3.21 整組閾值是同一個正常值乘上係數

2026-08-02 §5A 抽取發現。`heparanase.md` §5.1 的血漿標記表：

| 標記 | 正常值 | 切點 | 比值 |
|---|---|---|---|
| Syndecan-1 | < 40 ng/mL | > 80–100 ng/mL | 2–2.5 |
| Heparan sulfate | < 300 ng/mL | > 600 ng/mL | **2** |
| Hyaluronan | < 50 ng/mL | > 100 ng/mL | **2** |
| Soluble thrombomodulin | < 5 ng/mL | > 8 ng/mL | 1.6 |

正常值全是整十或整百，四個切點裡三個恰為正常值的兩倍。

〔推論〕**ROC 分析不會成組產出這種數字。** 不同族群、不同終點、不同研究算出的
最佳切點，落在正常上限的整數倍上是可能的，四個裡有三個同時如此則不太可能是巧合。
較可能的成因是：先有一組正常值，再乘一個係數推出切點，
然後把整組數字寫成像是各自有出處的樣子。

與 §3.9（定性描述被換算成具體數值）同族而方向相反——
§3.9 是「有描述、沒數值，於是造一個」，本型是「有一個數值、需要第二個，於是乘一個係數」。
共同點是**產出的數字在形式上完全合格**。

**查證程序上的用法**：遇到成組出現的閾值，先算比值再檢索。
比值整齊就先假設它們同源，查到其中一個的原始出處通常可一次解決其餘幾個；
反之若各自查各自的，會得到四份「查無出處」而看不出它們的關聯。

〔注意〕（2026-08-07 回填）**本型已有第二個實例，且已查證。**
`heparanase.md` §6.4 的七個誘導倍數（5–20、5–15、10–50、10–30、5–10、5–20、10–100）
全部無出處，八條回填判為 `unsupported`。文獻實測的 heparanase 誘導幅度分布於
1.49–8.9 倍，**該表的下界與實測上界相當，上界高出一個數量級**。

〔推論〕本例與 §3.21 原型的形狀不同：原型是「有一個數值、需要第二個，於是乘一個係數」，
本例是**整組數值的量級與該領域的實測分布不相容**。七個因子的機制迥異
（荷爾蒙、細胞激素、缺氧、轉錄因子），卻全部落在 5–100 這個窄帶且都以整數為界——
真實的跨研究彙整會參差，不會這麼齊整。

**查證程序上的用法擴充**：成組數值除了算比值，還要**與該領域的實測分布比量級**。
分布資料記於 `queries.md` §4.1。

〔注意〕本例疊了第二層：40 ng/mL 在本帳本已有另一個角色——
`5.1-sdc1-cutoff-40`（敗血症死亡率預測切點，已判 `partial`）。
同一個數字一處是健康人的正常上限、一處是死亡預測切點，
而該列查證取得的敗血症實測基礎值（約 103 ng/mL）與兩者都不合。

---

### 3.23 規格寫了，檢查沒驗

2026-08-02 §6B 開工掃描既有列時發現：**§5B 的 37 列 `source_ref` 寫的是裸節號 `5.5`，
而不是 `glycocalyx/heparanase#5.5`**。成因是寫入腳本把來源節號的常數定義成裸節號
而未加檔案前綴，治療總表那一批的每一列都套用了它。

〔注意〕**這批東西通過了全部七項驗收。** 列數、欄數、排序、無重複、無 BOM、無 CRLF、
SCHEMA §9 四欄值域檢查全部通過——因為 §9 檢查的是 `claim_type`／`status`／`tier`／
`priority` 四個封閉值域，而 `source_ref` 是自由文字欄，沒有任何檢查涵蓋它的格式。

〔推論〕這與 §7 記錄的兩起事故（v8 的 `fact` 值不在合法值內、2026-07-30 的編碼損毀）
同族——*所有既有檢查都通過而東西是壞的*——但成因更基本。
前兩者是**規格與實作不一致**；本例是 `SCHEMA.md` §2 明確定義了 `source_ref` 的格式
（`{目錄}/{檔名}#{節號}`），而 §9 的驗收條件**沒有把它納入**。規格寫了，檢查沒驗。

〔推論〕值得注意的是這個缺陷**不影響任何既有檢查會看的東西**，
但它會在回填時才發作：`source_ref` 是回填時回頭找原文的唯一座標，
裸節號 `5.5` 在三份來源文件裡都存在，指向三個不相干的地方。
若不是本批開工掃描時碰巧看到，它會一直潛伏到有人依 `source_ref` 找原文為止。

**處置**：37 列已於 §6B 修復。修復本身無爭議——SCHEMA §2 已有明文格式，
37 列與之不符，改回符合格式不涉及判斷，故直接執行未停下等核准。

**已於 2026-08-07 核准並執行為 schema v10。** 條文見 `SCHEMA.md` §9。原提案如下：

```python
[r['claim_id'] for r in rows
 if not all(('/' in t and '#' in t) for t in r['source_ref'].split(';'))]
```

輸出須為空。這會是 schema v10——不改欄位結構，只增加一項驗收條件。

〔推論〕更一般的教訓：**每次為欄位寫下格式規定時，同時問一句「哪一項檢查會驗它」。**
`SCHEMA.md` 目前有明文格式規定而無對應檢查的欄位還有 `claim_id`（§3 的命名規則）
與 `evidence`（§4 的 token 形式）。兩者尚未出過事，但成因結構相同。

---

### 3.22 整段被歸類略過時，段內的數值一併消失

2026-08-02 §5A 抽取發現（由兩次獨立抽取的比對揭露，見
`reports/extraction/2026-08-02-heparanase-5a-crosscheck.md` §2.3）。

`heparanase.md` §5.4 是三線篩查流程。流程順序本身不可證偽，
第一次執行時整段判為建議性而略過——連帶漏掉第二線那一句夾帶的
「舌下 GlycoCheck（PBR > 2.0 μm）」，那是一個與 §5.1（> 2.0–2.2 μm）
不同的閾值，應該建列。

〔推論〕本型與既有的抽取失效模式不同層。§3.18（模態被強化）、
「整列表格抽成一條」都發生在**候選已經被選出來之後**；
本型發生在**候選被選出來之前**——整段在掃描階段就被歸類掉，
段內的可證偽片段沒有機會進入逐句檢查。

**規則**：判為建議性、修辭性、流程性而整段略過時，仍須逐句掃一次數值。
**已於 2026-08-07 寫入 `runbooks/extraction.md` §8 作為第六個已知失效模式。**
同批另把「整欄不建列的三種欄位」（未來預測／無操作定義的程度估計／作者自訂分級）
寫入該檔 §6，與劑量欄並列。

〔注意〕（2026-08-02）**規則產生後的第一批就命中四次。** §5B 的 `5.6` 治療階梯
與 `5.7` 監測建議都是整段建議性內容，依本規則逐句掃出四條可證偽片段：
「糖萼修復可疊加」、「重症醫學標準方案」、「目標：下降 > 50%」、「目標：PBR < 2.0 μm」。
最後一條正是 5A 比對時要求補上的 PBR 第三個閾值。
**待核准**：正式寫入 `runbooks/extraction.md` §8。

〔注意〕5B 另釐清一條相關分界：`5.7` 的「每 3-6 個月追蹤」**不建列**（療程安排，
落在 `CLAUDE.md` 第 6 條），而「下降 > 50%」「PBR < 2.0 μm」**建列**（治療目標值，
不是給藥或排程）。此分界本批第一次遇到。

〔注意〕同一次比對還揭露 §3.20 的檢查方式太窄。§5A 內有兩組「同一件事在
前言／表格／結論三處表述不同」的序列：一組差在**數值**（前導時間 6-12 個月
vs 數年），一組差在**模態與具名程度**（治療宣稱從「可部分恢復 + 具名兩類藥」
遞增到「能逆轉 + 現有藥物」）。第一次執行只認出前者，因為數值可以直接比大小。
§3.20 的檢查應從「找數值矛盾」擴大為「找同一件事在三處的表述差異」，
差異可能落在數值、模態、具名程度任何一個維度上。

---

### 3.24 一張表的證據基礎來自另一個領域

2026-08-09 `4.1.3` 訊號途徑欄回填確立。本型不是單列的錯，是**整張表的歸屬問題**。

`heparanase.md` §6.4「Heparanase 的上游調控」在本檔脈絡下讀起來是在談血管內皮，
但逐列查證後，支持這些調控關係的原始研究**幾乎全部來自腫瘤生物學**，
而凡是有血管內皮實驗可以對照的地方，方向就對不上：

| 調控關係 | 支持方（細胞） | 反駁方（細胞） |
|---|---|---|
| Egr1 → heparanase 上調 | 前列腺／乳腺／結腸癌 | 黑色素瘤（同篇，抑制） |
| VEGF → heparanase 上調 | 黑色素瘤 | **血管內皮**（分泌減少） |
| 細胞激素 → NF-κB → heparanase | 膠質瘤微膠細胞 | **血管內皮**（四種抑制劑皆無效） |
| AGEs → RAGE → NF-κB → heparanase | —（RAGE 段成立） | **血管內皮**（NF-κB 抑制劑無效，轉錄因子為 FOXO4） |

〔注意〕最後兩列的反駁來自**兩篇獨立研究、相隔七年、不同實驗室**
（`DOI:10.1021/bi0356552` 2004、`DOI:10.1007/s11010-011-0804-7` 2011），
結論一致：**內皮細胞中 heparanase 的誘導不經 NF-κB。**
這已經不是個別列的出處問題，而是該表把一條在腫瘤細胞成立的通用軸線
整批套到內皮上。

〔推論〕本型與 §3.6（機制歸屬錯到分子上）、§1.7（數值歸屬錯到器官上）同族，
但**尺度不同**：前兩者是一個數值或一個機制搬錯位置，本型是**一整張表的證據層**
來自另一個領域。逐列查證會把它顯示成七個獨立的 `unsupported`／`contested`，
只有把整張表的支持方與反駁方並排列出，才看得出它們共用同一個成因。

**查證程序上的用法**：一張表若連續三列以上的反駁文獻都指向**同一種細胞或組織**，
停下來檢查該表整體的證據來源領域，不要繼續逐列判。
判定照常寫入，但批次報告必須有一節記錄這個共同成因——
否則下一個人會把七列各自的 note 讀成七件事。

〔注意〕這也解釋了為什麼 §6 是全檔語氣最強的一節（見 §3.19）。
腫瘤 heparanase 文獻本身的效應量與確定性都高於內皮糖萼文獻，
照搬過來時，**語氣連同結論一起搬了**。

〔注意〕（2026-08-13 回填）**本型有第二個軸：終點。**
`6.3` 第四批確立。§5.5 治療總表「臨床證據等級」欄七列的具名依據，
其試驗終點分別為 MACE、體重、蛋白尿、視網膜滲漏、神經傳導、腫瘤療效——
**無一為糖萼**，而欄標題在該表脈絡下讀起來是「對糖萼保護的證據等級」。

| 藥物 | 具名依據 | 那些試驗的終點 |
|---|---|---|
| SGLT2i | 多項 CVOT、DKD、DR 子分析 | MACE、腎病變複合終點 |
| GLP-1 | LEADER、REWIND、STEP | MACE、體重 |
| Sulodexide | 多項 DKD、DR、微量白蛋白尿 RCT | 蛋白尿、視網膜滲漏 |
| Fenofibrate | FIELD、ACCORD-Eye | DR 進展 |
| α-硫辛酸 | 歐洲核准糖尿病神經病變 | 神經病變症狀與傳導 |
| 非抗凝肝素 | 動物完全阻止 DKD/DR、Phase II 進行中 | 腫瘤療效 |

〔推論〕**這些等級可能對它們原本的終點是正確的。**
SGLT2i 對心腎終點確實有 1A 級證據。錯的不是等級本身，
是等級被移植到另一個終點上。這與細胞類型軸的處置相同——標註不對位的維度——
故補記於此而非另立新條。

〔注意〕該欄十個項目中**只有一個**（Sulodexide，`PMID:20865240`）
有直接糖萼終點的人體資料，卻給了十個等級。
若欄位的意思真是「對糖萼保護的證據等級」，
其餘九個應落在最低等級或標為無證據，而不是分散在 1A 到 2C。

---

### 3.25 期望值被寫成實測值的形式，且排序與證據強度呈反向

2026-08-13 `6.3` 三個批次確立。本型與 §3.21、§3.9 同族（數字在形式上完全合格），
但辨識特徵不在數字本身，而在**整欄的排序方向**。

`heparanase.md` 目前有兩欄符合，分屬不同表格：

**§5.5 治療總表「修復幅度（動物/人體）」**

| 修復幅度 | 藥物 | 實際臨床證據 |
|---:|---|---|
| 20-40% | DOAC、螺旋藻 | — |
| 25-50% | GLP-1 | **有隨機分派試驗** |
| 30-50% | α-硫辛酸、Fenofibrate | — |
| 30-60% | SGLT2i | **有隨機分派試驗** |
| 40-70% | Sulodexide | **有人體研究** |
| 50-80% | FFP/白蛋白 | 綜述明載無敗血症臨床研究 |
| 70-90% | 非抗凝修飾肝素 | **停滯於 Phase I，無糖萼終點** |
| 80-100% | 實驗階段三策略 | **全部前臨床** |

**§6.5 抑制策略表「抑制強度」**

中和抗體給 `+++++`（全表最高），而它是全表**唯一未進入人體**的類型；
天然抑制劑給 `+`，而該類查無任何 heparanase 抑制文獻。

〔推論〕**證據最少的拿到最高值**，這個方向不會出現在彙整自實測的欄位裡。
合理的解讀是這兩欄記錄的是**期望值或理論上限**——
「這個機制若成功，最多能做到多少」——但被寫成了與實測值同樣的形式
（百分比、符號），因此讀者無從分辨。

〔推論〕三個佐證：
1. §5.5 全欄邊界皆為 5 的倍數，跨距一律 20-30 個百分點，且**單調遞增**；
2. 十列中出現**兩組完全相同的區間**，而配對的兩者機制毫無關係
   （30-50% 是抗氧化劑與 PPAR-α 促效劑；20-40% 是抗凝血劑與 Nrf2 激活劑）；
3. §5.5 的排序與該表自身的「等級」欄一致——它其實是序位，不是測量。

真實的跨研究彙整不會這麼齊整，也不會讓機制無關的項目共用同一區間。

**查證程序上的用法**：遇到成欄的數值，先**排序並對照該欄項目的實際證據強度**。
方向相反就先假設整欄同源，改用抽樣定性（見 `queries.md` §8.16）而非逐列查證。
逐列查會得到十份「查無出處」而看不出它們是同一個構造的產物。

〔注意〕本型與 §3.24 的差別：§3.24 是**證據來自另一個領域**（有出處但領域錯），
本型是**根本沒有出處**（數字是構造出來的）。兩者都以整欄為單位，
但處置不同——前者要標註領域限定，後者要標註無出處。

---

### 3.26 檢查存在，但條件寫錯

2026-08-13 `6.3` 第三批發現。本型是 §3.23 的變體，且更難察覺。

§3.23 記的是「規格寫了、檢查沒驗」——`SCHEMA.md` 定義了 `source_ref` 的格式，
而 §9 的驗收條件沒有把它納入。本型的差別在於：**檢查是存在的，條件寫錯了。**

`6.3` 前兩批有三列違反 SCHEMA §7（「反駁（`!`）與不對位（`~`）文獻均不計入 `tier`」），
evidence 僅有 `~` token 卻填了 `tier`，而每一批的驗收都報 PASS。條件寫成：

```python
all(r['tier']=='' for r in rows
    if r['evidence'].startswith(('~','!'))
    and ';' not in r['evidence']
    and r['claim_id'] != '<某個特例>')
```

三個排除子句——只看開頭第一個 token、排除含分號的列、手動排除一個特例——
**剛好把要抓的對象全部排除掉**。

〔推論〕排除子句的成因幾乎總是相同：**檢查報錯了，而當下判斷那是誤報**。
但那正是最該停下來確認的時刻。本批一開始也遇到嚴格版報錯，
差別只在於這次先去查了 `SCHEMA.md` §7，才發現是資料真的錯。

〔推論〕與 §3.23 相比，本型的危險在於**帳面上有一項 PASS**。
§3.23 的缺陷至少會在「有沒有這項檢查」的清單上看得出來；
本型會顯示為綠燈，而綠燈比沒燈更難令人起疑。

**處置原則**：
1. **先寫最嚴格的版本跑一次。**
2. 報錯了逐一查證每個案例——是資料錯，還是規則真有例外？
3. 確認是例外才加排除子句，且**排除的理由要寫進報告或註解**。
4. **手動列舉的 `claim_id` 特例尤其可疑**——真正的規則例外應該能用條件描述，
   描述不出來就代表那不是例外，是缺陷。

〔注意〕本次三列已更正並全表掃描，另有 5 列為混合 token 且有 `tier`，
逐一確認 `tier` 皆來自無前綴 token，合規。
嚴格版條件已寫入 `runbooks` 的交付驗收清單與 `queries.md` §8.19。

〔推論〕§3.23 末段列出「有明文格式而無對應檢查」的欄位時提到 `claim_id` 與 `evidence`
「尚未出過事，但成因結構相同」。本次出事的是**第三個**——`tier` 與 `evidence` 前綴的關係，
那條規則寫在 §7 而非 §2，因此不在當初盤點的範圍內。
**盤點時要掃的是全部條文，不只是欄位定義那一節。**

---

## 4. 下一批

**當前焦點：`6.3` 剩餘 65 列（2026-08-13）。**

`6.3` 拆出的三個子節點中，`6.3.1`（20 列）與 `6.3.2`（8 列）皆已 100% 完成，
`6.3` 本身 16/81。§5.5 治療總表「修復幅度」欄十列已全數判定，全欄無出處。

**分批軸已改為「表格欄位」而非「藥物」。** 理由見 §3.25：
同一欄的列共享成因，逐欄處理可一次定性；逐藥處理會得到十份互不相干的
「查無出處」而看不出它們是同一個構造的產物。

下一個目標是 **§5.5 總表的「證據等級」欄**。判準已備妥——
依 §3.25 的程序，先排序該欄並對照各項目的實際證據強度；
方向相反即假設整欄同源，改用抽樣定性（`queries.md` §8.16）。
若成立即為 §3.25 的第三個實例。

---

**§2.2 已全部封閉。** 2026-07-29 B2b 批完成最後三條，
`section=2.2` 的 11 條全部離開 `unverified`：

```
verified     2   2.2-cap-0.2-0.5um
                 2.2-brain-mouse-0.54-0.23
partial      5   2.2-artery-2-3um
                 2.2-liver-porous-physiological
                 2.2-lung-thin-sdc1
                 2.2-pathological-50-90pct
                 2.2-repair-7-14d
contested    1   2.2-glomerular-hs-charge
unsupported  3   2.2-brain-1-3um
                 2.2-sepsis-shedding-30min
                 2.2-sdc1-halflife-2-8h
```

`2.1.1` 與 `2.2` 兩章至此無懸空條目。厚度、時間尺度、降幅三組定錨值已建立，
見 `queries.md` §2.1、§2.1.1、§2.1.2。後續任何章節出現這三類數值，先對照定錨值再檢索。

---

**優先於回填的兩件（2026-07-30）**：`runbooks/source-editing.md` 條文核准，
以及三個受追蹤檔的措辭執行單。見 §5「待核准」。
執行單完成後會產生約 58 條新列進入回填佇列，故建議在那之前先跑完下一批回填，
避免佇列在批次進行中變動。

---

**PBR 群已於 2026-07-30 完成**（`unsupported` 1 ／ `partial` 1）。
§3.3 通則沒有再次命中——兩條 statement 對 PBR 的方向都寫對了。
但回測給出一條更好用的篩選規則：**方向錯誤只出現在把 PBR 當「厚度數值」引用的地方，
不出現在把 PBR 當「指標」討論的地方**（見 `queries.md` §7.4）。

**therapeutic 群已於 2026-07-30 完成**（`partial` 4 ／ `unsupported` 2 ／ NEEDS-REVIEW 1）。
`claim_type=therapeutic` 的七條全部離開初始狀態，其中一條因程序缺口停在 `unverified`：

```
partial       4   2.4-precursor-insufficient
                  6.1-heparanase-inhibitor-target
                  6.1-metabolic-reprogramming-target
                  6.1-sulodexide-mixed
unsupported   2   4.2.3-hrt-rct-2025
                  6.1-microbiome-target
needs-review  1   5.4-s1p-preconditioning（status 維持 unverified）
```

本批 verified 為 0，這是預期的：SCHEMA §2 規定 `therapeutic` 不得以動物或體外證據
判 `verified`，而本領域的治療證據絕大多數止於動物。
治療類的陰性結果定錨值表已建立，見 `queries.md` §2.3。

**下一批建議：三檔的 §3.2 抽 claim（`heparanase.md` / `v3.md` / `axis.md`）**

理由：這是目前唯一擋在路上的大工程（約 54 行、預估新增 140–195 條），
且它會讓回填佇列大幅變動，越早做越好。前一批建議「抽 claim 那批別和 therapeutic 交錯」，
therapeutic 已完成，交錯的顧慮解除。

〔注意〕抽取前必讀本檔 **§3.18（模態被強化）**——那一型正是抽取環節產生的，
而這是本專案至今最大的一次抽取。逐條核對原文的「未必／可能／傾向於」，不得換成全稱。

**`axis.md` 已於 2026-07-31 抽完**（淨新增 16 條、追加 `source_ref` 4 條、
待建節點 0），報告見 `reports/extraction/2026-07-31-axis.md`。
該批兼作 schema v7 與 `runbooks/extraction.md` 的驗證，四個已知失效模式遇到三個。

**`heparanase.md` 五批進行中**，64 節，預估 120–170 條：

```
[x] 1.x      總論與慢性發炎機制         2026-07-31 完成，23＋10
[x] 2        糖尿病腎病變               2026-07-31 完成，26＋10
[x] 3        糖尿病視網膜病變           2026-08-01 完成，42＋7
[x] 4        糖尿病神經病變             2026-08-01 完成，43＋3
[x] 5A       早期診斷（L362-424）       2026-08-02 完成，41＋0
[x] 5B       治療與修復總表（L426-461） 2026-08-02 完成，46＋5
[ ] 6.x      肝素酶專論                 → 4.1.1–4.1.3 / 6.3
[ ] 7        糖萼以外病理               → 7.1–7.4
```

**§3 已於 2026-08-01 抽完**（淨新增 42、追加 `source_ref` 7、待建節點 0、
1 項暫置）。報告見 `reports/extraction/2026-08-01-heparanase-3.md`。
該批確立了平行章節的追加／新建判準（指名器官→新列；器官中性通則→追加），
並產生新的系統性問題 §3.20。

**§4 已於 2026-08-01 抽完**（淨新增 43、追加 `source_ref` 3、**待建節點 2**）。
報告見 `reports/extraction/2026-08-01-heparanase-4.md`。
三方並排確認了 §3.20 的模態遞增序列，並找到本檔第一個直接數值衝突
（heparanase 抑制劑的試驗期別，§3.5 一期 vs §4.5 二期）。
兩個待建節點的提案見 `reports/revisions/2026-08-01-topics-nonvascular-glycocalyx.md`，
相關 6 列暫置 `5.6.3`、2 列暫置 `5.6.2`／`5.6.3`。

---

**§5A 已於 2026-08-02 抽完**（淨新增 41、追加 0、待建節點 0）。
報告見 `reports/extraction/2026-08-02-heparanase-5a.md`，
另有同一節兩次獨立抽取的比對報告 `2026-08-02-heparanase-5a-crosscheck.md`。
該批產生 §3.21、§3.22 兩個新型態，以及一項待決（無操作定義的最高級如何處理）。

**§5B 已於 2026-08-02 抽完**（淨新增 46、追加 5、待建節點 0）。
報告見 `reports/extraction/2026-08-02-heparanase-5b.md`。
`heparanase.md` §5 全節至此抽完。

**§6B 已於 2026-08-02 抽完**（淨新增 51、追加 5、另修復 37 列 `source_ref` 格式）。
`heparanase.md` §6 全節抽完，全檔僅餘 §7。
報告見 `reports/extraction/2026-08-02-heparanase-6b.md`。

**`4.1.3` 回填已於 2026-08-07 完成**（8 條：`contested` 1、`unsupported` 7）。
報告見 `reports/backfill/2026-08-07-4.1.3.md`，檢索原始回傳於 `inbox/2026-08-07-4.1.3.json`。

**`4.1.3` 已於 2026-08-09 全節封閉**（16 列，0 `unverified`），
為本專案**第一個完成率 100% 的節點**。分布：

```
verified     1   4.1.3-egr1-direct-hpa-binding
partial      2   4.1.3-estrogen-er-hpa-promoter
                 4.1.3-angiotensin2-at1-pkc
contested    2   4.1.3-hyperglycemia-ages-rage-nfkb
                 4.1.3-tnfa-il1b-nfkb
                 4.1.3-vegf-positive-feedback
unsupported 10   （七個誘導倍數 + hif1a 結合 + tnfa-il1b-mapk）
superseded   1   4.1.3-tnfa-il1b-nfkb-mapk（拆列）
```

（`contested` 實為 3 條，上列 status 標頭沿用 §1 的計數規則，以 §1 為準。）

〔注意〕**16 列中只有 1 列 `verified`，而那一列是全表唯一不含數值、不含方向詞的宣稱。**
節點完成不等於節點可信——這是第一個 100% 節點給出的第一個訊號。
成因見 §3.24。

〔注意〕本批確立的程序要求已寫入 `queries.md` §1.15：
**成組主張分批查證時，後一批結束後必須回頭掃前一批的列。**
本批據此補了兩個 `!` token，其中一個是 §6.4 那一格至今唯一的實測值（內皮細胞 2-10 倍）。
後續批次報告固定加一節「對前批列的附帶修訂」，沒有就寫「無」。

---

**回填節奏已於 2026-08-09 定案（方案 1 ＋ 2），條文見 §5「已決 — 回填與抽取的節奏」。**

現況數字：`high` 未判定 236 條、`med` 未判定 160 條，一批 5-8 條。

**下一批：`5.5` 回填（標記效能）。** 依定案條文，§7 開抽之前先跑這一批。
選 `5.5` 的理由是 `high` 密度最高，且該節點的 18 列多為切點與前導時間——
兩者都是 `queries.md` §1.2、§1.4、§7.1 已建立過檢索法的類型，
成組取列的效益在這裡最明顯。

〔注意〕依同一份定案，本批結束時**必須回掃 `5.1`／`5.3`**——
`5.5` 與那兩節共用 syndecan-1 與 PBR 這兩組數值，
`2026-07-26-5.5.md` 與 `2026-07-30-5.3-5.5-pbr.md` 兩份舊報告的判定應一併對照。
批次報告固定加「對前批列的附帶修訂」一節。

〔注意〕`6.3` 的拆分提案已寫成 `reports/revisions/2026-08-09-split-6.3.md`，**待裁示**。
該提案不阻擋 `5.5` 批次，但**應在 §7 開抽之前決定**——
§7 只會往 `6.3` 加同一種量綱，等它抽完再拆，分母更大而混雜結構不變。

〔注意〕**§7 抽取前應先提案拆 `6.3`。**
〔注意〕**§7 抽取前應先提案拆 `6.3`。** 該節點現有 109 列，量綱已達五種：
某藥在某器官的試驗結果、證據等級、糖萼修復幅度、監測目標值、藥物開發狀態。
最後一類（期別、公司、試驗編號、終止與否）的查證來源是註冊資料庫而非期刊文獻，
與前四類不同。等 §7 抽完再拆不會讓判斷更清楚——§7 不改變量綱組成。

〔注意〕§7 抽取時須回頭標註三處：`4.1.3-egr1-direct-hpa-binding`
（§7 的 AMPK/SIRT1→Egr1 路徑為同一節點的上游）、
`6.3-sglt2-indirect-strongest-protector`（§6.8 稱 SGLT2i 為間接最強糖萼保護藥，
§7 全節即為該列的詳述）、`6.3-sglt2-strongest-heparanase-suppression`（§5.5 總表）。

---

**原下一批說明（已完成）**

〔注意〕§6 含三張表：`6.2` 的正常生理功能表（6 列 × 2 欄）、
`6.4` 的上游調控表（7 列 × 3 欄，含誘導倍數）、`6.5` 的抑制策略表（5 列 × 4 欄），
另有 `6.7` 的臨床試驗管線總表（6 個藥物 × 7 欄，本檔最寬的一張表）。
依 §3.22，`6.6`／`6.8` 兩段結論即使判為修辭性，略過前仍須逐句掃數值。

〔注意〕§6.7 的管線表會第四次觸及 heparanase 抑制劑的試驗期別。
本檔已有三個值（§3.5 一期、§4.5 二期、§5.5 總表「Phase II 進行中」），
見 §3.20。管線表逐藥列出期別，是判定哪一個值正確的最佳位置。

〔注意〕§7（SGLT2i 專論）會再加入數十列到 `6.3`。該節點目前 72 列，
是本帳本最大者，且橫跨四種量綱（某藥在某器官的試驗結果、證據等級、
修復幅度、監測目標值）。若 §7 後超過 100 列，`6.3` 會失去分批的實用性，
屆時應提案拆節點。本批未提案。

---

**原下一批說明（已完成）**

〔注意〕5B 的追加 `source_ref` 數會回升。治療總表十項策略中，SGLT2i、GLP-1 RA、
Sulodexide、fenofibrate、α-硫辛酸五項在 §2.5／§3.5／§4.5 已有列，
多數應追加而非另建列——先以藥名為鍵掃過整份 CSV 再決定。

〔注意〕`5.6`（治療階梯）與 `5.7`（監測建議）依 `CLAUDE.md` 第 6 條
**不得為劑量、給藥途徑或療程建立 claim**。治療總表的「主要適應症與劑量建議」欄
整欄含劑量，該欄一律不建列，劑量資訊寫入 `note` 供追溯。

〔注意〕5B 須補上 PBR 的第三個閾值（§5.7「目標 < 2.0 μm」），
與本批的 `2.1.2-pbr-cutoff-2.0-2.2`、`2.1.2-pbr-screening-cutoff-2.0` 互相標註。
同一個 2.0 在三處分別是區間下界、篩檢閾值、治療目標。

〔注意〕依 §3.22，5B 的 `5.6` 治療階梯是整段建議性內容，
**略過前須逐句掃一次數值**——該段夾帶 `250 LSU bid`、`10–20mL/kg` 等劑量
（依第 6 條不建列）以及非劑量的可證偽片段。

---

**原拆批說明（2026-08-02 核准）**

```
5A  L362-424   §5.1–§5.5 診斷方法        主要落在 5.7 / 5.5 / 2.1.2   預估 30-40 條
5B  L426-461   治療總表 + §5.6 + §5.7    主要落在 6.3 / 6.1           預估 15-25 條
```

拆的理由是兩半的失效模式不同：5A 的風險是表格列被整列抽成一條 claim，
5B 的風險是重複建列（五種藥在 §2.5／§3.5／§4.5 已有列）與劑量入列。
混在一批會使報告的檢查段落失焦。

〔注意〕**這是本檔數值最密集的一節**，含三個表格：`5.1` 的血漿標記切點表
（4 個標記 × 正常值與切點）、`5.3` 的新技術表、以及 `5.5` 與 `5.6` 之間的治療總表
（10 個治療策略 × 6 欄）。依 `runbooks/extraction.md` §2，
**表格每一列都要單獨看**，不得把一整列抽成一條 claim——
既有帳本中相當比例的問題列來自表格。

〔注意〕`5.6`（治療階梯）與 `5.7`（監測建議）依 `CLAUDE.md` 第 6 條
**不得為劑量、給藥途徑或療程建立 claim**。治療總表的「主要適應症與劑量建議」欄
整欄含劑量（`empagliflozin 10–25mg/d`、`250 LSU bid`、`600–1800mg/d` 等），
該欄一律不建列，劑量資訊寫入 `note` 供追溯。

〔注意〕前置對照（§4 批次已標註）：

- `5.1` 的 syndecan-1 切點（`> 80–100 ng/mL`）是第三個切點值，與
  `5.1-sdc1-cutoff-40`（敗血症 40）、`5.6.1-dkd-sdc1-100-4-6x`（DKD 100）並存
- `5.3`／`5.4` 的 PBR 切點須對照 `2.1.2-pbr-inverse` 與 `5.5-pbr-sdc1-correlation`，
  並注意 §3.3（逆向指標被寫反）——PBR 是逆向指標
- 治療總表的 fenofibrate、α-硫辛酸、sulodexide、SGLT2i、GLP-1 RA 五項
  在 §2.5／§3.5／§4.5 已有列，多數應追加 `source_ref` 而非另建列
- `5.3` 的角膜神經糖萼「早 2-5 年」須與 `5.6.3-dn-ccm-earlier-than-fiber-density`
  互相標註（該列的來源節未給前導時間）
- `6.3-sglt2-glp1-restore` 已在 `1.5`、`2.5`、`3.5`、`4.5` 四處追加，
  第一批預告的五處只剩 `7` 未到

〔注意〕§5 的接縫前言（L362-363）與 §3、§4 同型，處理方式沿用
`5.6.3-dn-earliest-event`。

〔注意〕**本批是 schema v9 生效後的第一批**，而 §5 整節就是
「具名量測平台 + 人體患者」的集合（SDF／GlycoCheck、CCM、流式細胞儀、雷射散斑、
Dextran-40 體積測量、多光子顯微鏡）。依 v9 人體閘門，這類列一律 `epidemiology`；
主詞為平台本身（規格、原理、取樣方式、正常參考範圍）者才是 `measurement`。
抽取時逐條先過閘門再問型別，不得依「statement 內有具名平台」直接填 `measurement`。

〔注意〕**`5.3` 新技術表按欄分流**（2026-08-02 核准）。該表四項技術 ×
「原理／預計上市時間／敏感度預估」三欄：

- 「原理」欄可證偽（例：多光子顯微鏡能直接觀察腎小球糖萼厚度）→ 照常建列
- 「預計上市時間」（2026–2028）與「敏感度預估」（極高／高）**不建列**——
  前者是對未來的預測，後者未給可比對的量，兩者現在都無法判真假。
  數值寫入該列的 `note` 供追溯，並在批次報告的 skipped 段落記明理由

理由與 `CLAUDE.md` 第 6 條處理劑量欄同型：不建列但不失去痕跡。
若照常建列，會產生一批永遠停在 `unverified` 而且**不是因為還沒查**的列，
污染完成率的分母。

〔注意〕5B 的治療總表有一欄「糖萼修復幅度」把動物與人體數據寫在同一格
（`30–60%`、`40–70%（人體最高）`、`急性期50–80%`），
是 §3.11（多宣稱列的 tier 失真）與 §3.15（不同單位的兩個量被併成一個範圍）的組合型態。
逐格拆開判斷來源物種再決定 `tier`，不得整格抽成一條。

〔注意〕`heparanase.md` 是三檔中承載措辭最密集者（「2025 國際共識」「頭號兇手」
「唯一能剪斷 HS 的內切糖苷酶」），依 SCHEMA §6（v4）這些一律不得進 `statement`，
須改寫為可證偽形式並把原措辭記入 `note`。這會使該檔的抽取比 `axis` 慢。

〔注意〕`TOPICS.md` 的節點覆蓋率在 `heparanase` 不會像 `axis` 那樣達到 100%
（`axis` 的待建節點為 0，是因為 `TOPICS.md` 的新節點就是照它規劃的）。
遇到沒有對應節點的主題，**不得自行造節號**——記入批次報告的「待建節點」，
該批結束後一併提案。

---

**前置作業（已完成，保留紀錄）**：schema v6 兩案均採選項 A。
抽取時可直接套用的新程序：

- 遇到混合宣稱列 → SCHEMA §6「拆列程序」，**先拆再判**
- 遇到不對位文獻 → SCHEMA §4「`~` 前綴」，`note` 須寫明不對位維度
- 兩者的注意事項見 `runbooks/backfill.md` 步驟 4 新增的並行分支與拆列檢查

**抽取前必讀清單**（依序）：本檔 §3.18（模態被強化，這是抽取環節特有的錯誤）、
SCHEMA §6（statement 撰寫要求＋拆列程序）、SCHEMA §2（認定規則，決定追加
`source_ref` 還是建新列）、`CLAUDE.md` 第 6 條（不得為劑量建 claim）。

**檢索前必讀**（回填時）：`queries.md` §1.4（三次措辭）、§1.9、§1.10、§1.11、
§8.1（治療類先查陰性）、§8.4（介入被換掉時的反查），
以及 §2.1 定錨值表末的頸動脈對照列與 §2.3 的治療類陰性定錨值表。

**現成線索（承接，尚未動用）**：`Okada H, et al. Crit Care. 2017;21:261`（`PMID:29058634`）
比較連續型（心）、開窗型（腎）、竇狀型（肝）三種微血管的糖萼超微結構。
2026-07-28 批次僅透過 Ando 2018 的轉述得知此文，**未直接讀取，不得據以判定**。
§2.2 已封閉，此線索改留給日後的血管床型別複查。

---

**已完成的回填批次一覽**（避免重做）

- 2026-07-28：`2.2-brain-1-3um`（unsupported）、`2.2-lung-thin-sdc1`（partial）、
  `2.2-brain-mouse-0.54-0.23`（verified）、`2.1.1-mucin-o-glycosylation-aging`（verified）、
  `2.1.1-em-underestimate`（verified）
- 2026-07-29 A 批：`2.1.1-extreme-11um-brain-kidney`、`2.1.1-glomerular-4-11um`（均 unsupported）。
  **十一微米群三條全部封閉**
- 2026-07-29 B1 批：`2.2-liver-porous-physiological`（partial）、`2.2-glomerular-hs-charge`（contested）
- 2026-07-29 B2a 批：`2.2-sepsis-shedding-30min`（unsupported）
- 2026-07-29 B2b 批：`2.2-repair-7-14d`（partial）、`2.2-pathological-50-90pct`（partial）、
  `2.2-sdc1-halflife-2-8h`（unsupported）

`5.1-sdc1-sens-spec` 與 `5.1-sdc1-sens-90` 同批。

**收錄批次（新增，與回填分離）**

`source/glycocalyx/heparanase.md`（42 節）與 `source/mitochondria/axis.md`（5 節）
已歸位但尚未抽 claim。抽取時：

- 只建列、填 `source_ref`、`status=unverified`，**不做查證**
- `heparanase#5.6`（治療階梯）與 `#5.7`（監測建議）依 `CLAUDE.md` 第 6 條
  不得為劑量建 claim；藥物與結果的方向性關聯照常建列
- 依 SCHEMA §6（v4），statement 不得含時間形容詞或未具名的權威性宣稱。
  `heparanase.md` 的各節結論（`#2.6`、`#3.6`、`#4.6`、`#6.6`、`#6.8`、`#7.4`）
  密集出現「2025 國際共識」，抽取時逐條改寫為可指名的形式
- `mitochondria/axis#1.1`–`#1.3` 與 `glycocalyx/v3` §1.2 主題重疊，
  依 SCHEMA §2 認定規則先掃描既有列，命中者追加 `source_ref` 而非建新列
- 預估新增 140–195 條。抽完後 §1 的 high 完成率會下降，**這是預期的**，
  不得為了讓數字好看而略過任何一節

抽取完成後，回填順序仍依本節既有優先序，新文件不插隊。

**之後的優先序**（不依 claim_id 順序）：

1. ~~`5.3-pbr-precedes-cognitive-2-3y`~~ — 已於 2026-07-30 完成
2. ~~`5.5-pbr-sdc1-correlation`~~ — 同上
3. ~~七條 `claim_type=therapeutic`~~ — 已於 2026-07-30 完成。
   三檔抽 claim 後湧入的 therapeutic 淨新增（AAV/C1GALT1、empagliflozin、Endocalyx、
   heparanase 抑制劑、S1P）另成一批，**不得併入已完成的本批**
4. 標示「2025 年研究」的新宣稱群：果糖 ER 壓力、TMAO、微塑膠、S1P、ecSOD。
   〔注意〕TMAO 與 S1P 兩項在 therapeutic 批次已有部分背景：
   TMAO 對內皮的文獻終點不含糖萼（見 `6.1-microbiome-target` 的 note），
   S1P 的 IRI 直接實驗為陰性（`PMID:32017300`）。查證前先讀那兩條的 note，可省數次檢索
5. 其餘 `mechanism` 類——查證相對機械，適合批次自動跑
6. `intake/2026-07-28-糖萼層生理` 的六糖胺途徑方向可疑句
   （「過度活化：消耗 UDP-GlcNAc」——過度活化應為產生而非消耗）。
   於 therapeutic 批次浮現，屬 §3.3 的方向型但主體是代謝通量，三檔抽 claim 時單獨建列

---

## 5. 待決事項

### 已定調的政策

**新增兩條漂移模式 §3.25、§3.26**（2026-08-13 裁示）。
兩者皆已累積三個以上佐證，且都改變後續批次的作業方式：

- **§3.25**（期望值被寫成實測值形式，排序與證據強度呈反向）
  → 改變**分批軸與檢索策略**：成欄數值先排序對照證據強度，
    方向相反即改用抽樣定性，不逐列查。
- **§3.26**（檢查存在但條件寫錯）
  → 改變**驗收寫法**：先寫最嚴格版本跑一次，報錯逐一查證是資料錯
    還是規則有例外，確認是例外才加排除子句且須寫明理由。
    手動列舉的 `claim_id` 特例視為缺陷而非例外。

〔推論〕這兩條是本專案少見的「對自己的作業程序」而非「對原文」的發現。
§3.26 尤其如此——它記錄的是驗收機制本身的失效模式，
而驗收是整個帳本可信度的最後一道關。

**節點 `6.3` 依查證來源拆為三個子節點，`6.3.3`（證據等級）不建**（人工裁示 2026-08-13）。
提案 `2026-08-09-split-6.3.md` §3 全案核准，§4 第 1 點（另寫註冊類判定準則）核准，
已寫成 `runbooks/backfill.md` §3A；§4 第 2 點（`6.3.3`）否決，
理由是證據等級主張沒有過期問題，拆出來得不到 `6.3.1` 那樣的排程效益，
且與療效列高度耦合。

**註冊類主張的過期以 `stale_after` 欄承載，不以 `status` 承載**（schema v12，2026-08-13）。
時間經過不是證據，逾期不降級，只提前回填順位。詳見 SCHEMA §13。

〔推論〕拆分與 v12 是同一件事的兩半。拆節點解決「查證流程不同」，
`stale_after` 解決「判定會過期」，而後者的必填規則必須以節點為範圍才可機器檢查——
不拆的話，109 列裡只有 20 列受規則管轄，只能逐列人工判斷。

**查無出處的數值一律標註，不從原文刪除**（人工裁示 2026-07-27，格式核准 2026-07-28）。
標註格式規範見 `reports/revisions/2026-07-27-source-2.1.1-thickness.md` §2，
三種標記 `〔查無出處〕`／`〔條件限定〕`／`〔有爭議〕` 對應 ledger 的 status。
`verified` 與 `unverified` 均不標註——尚未檢索不等於有問題，標了會稀釋真正的警示。
此政策**推翻**了 5.5 批次報告中「建議直接刪除敏感度／特異度欄位」的建議。

**標記不得刪除或改寫原句。** 原句原樣保留，標記以獨立段落加在其後。
若同一段落需要三個以上標記，代表該段應整段改寫，另寫修訂單，不靠標記硬撐。
〔推論〕這條是政策能否成立的關鍵：允許「標註時順手改一下」，
標註與改寫的界線會在幾次批次內消失。

**表格的標註格式：新增「查證狀態」欄，不用儲存格內尾隨、不用表格下方附註**
（人工裁示 2026-07-29）。原有欄位與所有數值一字不動，標記寫進新增欄。
`2026-07-27-source-2.1.1-thickness.md` §2 是為散文段落寫的，提到表格時只寫了禁止事項
（不得只寫在表格下方附註列），沒有正面規定。本條補上該缺口。

〔推論〕選新增欄而非儲存格內尾隨，理由是**它讓「整表皆中」一眼可見**。
標記散在儲存格裡讀者要逐格找；獨立成欄時，整欄滿的視覺效果本身就是訊息。
代價是動了表格結構，故每次仍須個別核准。

**「段落」在表格上以整張表為單位**（人工裁示 2026-07-29）。
因此四列的表有四個標記即觸發「三個以上」的改寫條款，而非視為四個各自獨立的段落。

**此規範不回頭改寫 `2026-07-27-source-2.1.1-thickness.md`。**
該檔為已核准提案，依 §3.8 屬歷史紀錄，改了就失真。新規範以本節為準。

**2026-07-30 起，上述標註規範已收攏至 `runbooks/source-editing.md` §2。**
本節與 `2026-07-27-source-2.1.1-thickness.md` §2 均維持原狀不改，執行時以 runbook 為準。

**原文措辭依「措辭問題／出處問題」分流處置**（人工裁示 2026-07-30）。
提案 `reports/revisions/2026-07-30-rhetoric-policy.md`，五項全數核准。

掃描報告把「時間形容詞 136 處」記為一類，但它其實是兩種東西：
純評價性形容詞（「最新」「近年」）刪掉零損失；年份＋文獻類型（「2025 年的薈萃分析」）
的年份是該句**僅存的溯源線索**，刪掉等於銷毀查證對象。

拆開後三類收斂成兩種處置：

| 類 | 處置 | 受追蹤三檔的處數 |
|---|---|---:|
| 純評價性形容詞 | 直接刪除，不加標記、不重寫句子 | 52 |
| 年份＋文獻類型 | 原句不動，抽成 claim 去查 | 26 |
| 未具名權威宣稱 | 同上 | 22 |
| 無主詞措辭 | 同上 | 10 |

後三類**不需要任何新規範**——SCHEMA §6 已明文「把宣稱本身建成 claim，
查不到即 `unsupported`」，接上回填流程與標註政策剛好覆蓋。
真正需要新規範的只有第一類。

範圍限 `source/` 受追蹤三檔；`intake/` 三檔一律不改（理由同 §3.8）。
一句同時命中多類時，以最重者為準：權威 > 年份 > 無主詞 > 形容詞，不做半句刪除。

〔推論〕這件事的教訓不在措辭上：**掃描報告的分類本身會決定選項的形狀。**
合併計數時，決策看起來是「要不要改 136 處」；拆開後才看得出措辭問題只有 52 處，
另外 84 處根本不是措辭問題。清點的類別粒度錯了，後面所有選項都會跟著錯。

**不得為具體劑量、給藥途徑或用藥時程建立 claim**（人工裁示 2026-07-28）。
劑量寫入 `note` 供追溯，不進 `statement`、不取得 `status`。
`claim_type=therapeutic` 的 statement 只描述方向性關聯。
完整條文見 `CLAUDE.md`「不可違反」第 6 條。

理由：`verified` 是文獻學意義的「有出處」，不是臨床意義的「已驗證可用」。
這個語意落差在劑量上會造成實際傷害。網頁上線前，`therapeutic` 的讀者層呈現須另行設計。

**單一 repo、單一帳本，主題以 `source/` 子目錄區分**（人工裁示 2026-07-28）。
不為糖萼、線粒體等主題各自開 repo。理由：分 repo 不減少查證工作量，只縮小分母；
且會把「一條主張一列」原則搬到跨 repo 層級，失去 `source_ref` 的去重機制。
糖萼研究的高價值內容多半長在主題**介面**上（糖萼—線粒體、糖萼—凝血、糖萼—免疫），
依主題切分，切線正好穿過最有價值的地方。

網頁仍分站，由 build 腳本依 `source_ref` 的子目錄前綴從單一 `claims.csv` 產生。
介面主題的 claim 在多站出現，但指向同一 `claim_id`、同一份判定。

**判定綁 claim，不綁文件**（人工裁示 2026-07-28，schema_version 3）。
同一主張在多份文件出現時只建一列，所有出現位置寫進 `source_ref`。

**但兩種情況不得合併，須各自建列**：數值相同而**歸屬不同**、
或同一指標**數值不同**。理由見 §3.6——漂移本身就是證據，
合併會把它壓進 `note` 而失去可見度。完整規則見 `ledger/SCHEMA.md` §2「一條主張，一列」。

### 待核准 — 原文修訂

- **`2.1.1-extreme-11um-brain-kidney` 等四條拆列在原文無對應句。**
  這些主張只出現在 `intake/` 文件，`source/glycocalyx/v3.md` 無對應段落可標註。
  網頁上線時，intake-only 的 claim 如何呈現須另行設計。

### 觀察中 — `evidence` 承載不了「相關但不對位」的文獻（2026-07-29 新增）

`2.2-sepsis-shedding-30min` 查到四筆高度相關文獻，但沒有一筆能填進 `evidence`：
最接近的那篇取樣點不夠早、不足以反駁；缺血再灌流那兩篇反駁的是歸屬而非現象。
四筆全部落進 `note` 的自由文字，無法被機器讀取或跨列比對。

而在這個專案裡，**不對位的文獻往往才是最有資訊量的那一筆**——它指出原主張從哪裡漂過來。

**2026-07-29 B2b 累加**：`2.2-sdc1-halflife-2-8h` 同型。
查到的四筆 HSPG 周轉文獻（`PMID:1533636`、`PMID:11755208` 等）給出了 2-8 小時這個量級
真正屬於的對象，但它們既不支持也不反駁「syndecan-1 的半衰期是 2-8 小時」——
量測的是別的分子、別的細胞。同樣只能落進 `note`。

**2026-07-30 第五筆**：`5.3-pbr-precedes-cognitive-2-3y` 的 `PMID:39494362`。
它既不支持也不反駁——量的是別的分子（CSF N-醣鏈）、用別的方法（ELLA 非 PBR）、
給的是別的量（軌跡提前量非前導時間）。但它很可能就是漂移的來源，資訊量最高。

**2026-07-30 第六筆**：`4.2.3-hrt-rct-2025` 的 `DOI:10.14814/phy2.70428`。
年份、規模、設計、族群、終點、方向六項要素中五項與原主張相符，只有介入不對
（實為糖萼標靶補充劑，且母試驗排除 HRT 使用者）。它既不支持也不反駁，
但幾乎確定就是原主張的來源。

〔推論〕這一筆對提案的**語意範圍**有直接影響。前五筆的共同點是「量錯了別的東西」，
本筆是「量對了東西但用錯了藥」。若 `~` 定義為「量測對象不同」會漏掉本筆；
須定義為「**不足以支持或反駁本主張，但與其來源相關**」才涵蓋得住。

**已核准並執行（2026-07-31，schema v6，選項 A）**：
`reports/revisions/2026-07-30-schema-nonaligned-evidence.md` §7 為執行紀錄。
四列已回填 `~` token，`status` 均維持 `unsupported`（`~` 不影響 `status`）。

本節保留以下三點作為歷史紀錄，因為它們是提案階段才浮現的、且都已寫進 SCHEMA：

1. **`tier` 的規則必須同步改。** 現行寫「未加 `!` 前綴」，若不改成「未加任何前綴」，
   `~` 會產生與 v2 修掉的那個 bug 完全同型的失效——
   `4.2.3-hrt-rct-2025` 的來源是一篇 RCT 事後分析，照字面會把一條 `unsupported`
   標成 `tier=rct`。v2 修的是「反駁越有力 tier 越高」，這裡是「來源找得越準 tier 越高」
2. **主要引用的規則必須同步改。** `evidence` 僅含 `~` token 的列沒有主要引用，
   否則前端會把一篇證明該主張沒有出處的論文顯示成它的代表文獻
3. **`~` 不得成為引用轉述文獻的後門。** `queries.md` §1.6 對 `~` 同樣適用

**執行時唯一沒照提案走的地方**：`PMID:29058634` 未寫入
`2.2-sepsis-shedding-30min` 的 `evidence`。該識別碼的來源狀態不一致
（本檔 §4 記為「僅透過 Ando 2018 轉述得知」，但該列 `note` 載有該文的逐點數值），
核准時未一併裁示，故依 §4 的「後門」條款暫不填入。**此項仍為待決，見下。**

### 待決 — `source_ref` 機械回填的誤配與移除程序（2026-07-29 新增）

`2.2-pathological-50-90pct` 的 `source_ref` 為
`glycocalyx/v3#2.2;intake/2026-07-28-糖萼層生理#L92`。實查該 intake 檔：

- L92 是「***病理情境***：」小節標題，該節談敗血症數小時內崩解，**不含 50-90% 敘述**
- 全檔的 50-90% 只出現兩處（L28、L154），都是 **HS 佔總 GAG 的組成比例**
- 全檔唯一的病理厚度變化敘述是「老化每 10 年減少約 10%」

依 SCHEMA §2 認定規則，組成比例與厚度降幅是不同的量，不得視為同一條主張的另一出處。

〔推論〕成因應是 2026-07-28 機械化回填時以**節區主題**（「病理」）而非**數值**為鍵匹配。
若成因如此，另外 52 條同時標示兩處的列可能有同型誤配。

須人工決定兩件事：

1. **是否做一次以數值為鍵的全庫校驗**（53 條雙出處列）。建議做——
   `source_ref` 是 §3.6 漂移偵測的基礎，掛錯的出處會讓兩份文件看起來說了同一件事
2. **移除誤配 token 的程序**。SCHEMA §7 的 append-only 只寫了列與 `evidence` token，
   `source_ref` 的移除無明文。與下方「混合宣稱列的拆列程序」同屬程序缺口，
   建議一併寫進 SCHEMA §2

**本批未修改該欄**，維持原值等待裁示。

---

### 待決 — 混合宣稱列的拆列程序（2026-07-29 新增）

`2.2-glomerular-hs-charge` 一列含三個判定結果不同的子宣稱，違反 SCHEMA §6
「一列一個主張」。建議拆為三列，`claim_id` 草案見
`reports/backfill/2026-07-29-2.2-b1.md`。

須人工決定的是**拆列的程序**，不是要不要拆：
SCHEMA §3 規定 `claim_id` 永不重用、永不改名，§7 規定不得刪列。
原列的處置應為 `status=superseded` 並在 `note` 寫明三個新 `claim_id`，
但「拆列」這個動作在 SCHEMA 無明文，`superseded` 原本設計給「被更強證據取代」，
語意不完全相同。

〔推論〕這條決定會重複出現——混合宣稱列不會只有這一條。
建議一次寫進 SCHEMA §6 作為通用程序，而不是逐條處理。

**2026-07-30 第二個實例，且這次擋下了判定。** `5.4-s1p-preconditioning` 的兩個子宣稱
（減輕 IRI 脫落／保護移植器官）判定結果相反：前者被 `PMID:32017300` 直接推翻
（S1P 預處理縮小梗塞面積但對 syndecan-1 無影響，作者明言保護非由糖萼介導），
後者查無原始研究。依 `backfill.md` §4 第 1 款（方向被推翻不自行判定）標記 NEEDS-REVIEW，
`status` 維持 `unverified`。

〔推論〕`2.2-glomerular-hs-charge` 是判完才發現混合宣稱，本例是判定前就卡住。
程序缺口的成本已經從「事後補記」變成「當場停工」，優先序應該提高。

**已核准並執行（2026-07-31，schema v6，選項 A）**：
`reports/revisions/2026-07-30-schema-split-row.md` §7 為執行紀錄與前後對照表。

`5.4-s1p-preconditioning` → `superseded`，拆為
`5.4-s1p-iri-shedding`（contested，tier=in_vitro）與 `5.4-s1p-transplant`（unsupported）。
拆完後這條主張的真實面貌才顯現：支持它的是不相干系統裡的體外實驗，
反駁它的是唯一一個真做了 IRI 的實驗——混在一列時這個對比被「另一半查無資料」稀釋掉了。

**執行時學到的一件事，已回寫進 SCHEMA §6**：原列的檢索紀錄**不自動及於子宣稱**。
`5.4-s1p-transplant` 的移植半句在 2026-07-30 只跑過一式，
依 `queries.md` §1.4 不足以判 `unsupported`，2026-07-31 補跑兩式後才寫入。
混合宣稱列的檢索深度在各子宣稱之間本來就可能不均，而原列的 `last_checked`
看起來像是整列都查過了。

**`2.2-glomerular-hs-charge` 的拆解保留未執行**，等 `PMID:29058634` 的來源狀態裁示，
以免把來源存疑的 token 複製到新列。

本案**不涵蓋** `source_ref` 誤配的移除程序（見上一則待決）。
兩者同屬 append-only 的程序缺口，但判斷基準不同，合併會變成包裹表決。

### 待核准 — `heparanase.md` 接縫前言（2026-07-30 新增）

9 行 17 處，需**句層切割**而非字層刪除，超出 `source-editing.md` §3 的範圍，
故未併入 2026-07-30 的執行批次。詳見
`reports/revisions/2026-07-30-source-heparanase-wording.md` §4。

多數行是混合的：框架句（「以下為……教授觀點，2025年11月最新……」）之後
緊接一個有實質內容的宣稱（L97「糖萼損傷是 DKD 最早、最核心的病理事件之一」、
L462「heparanase 是人體已知唯一能剪斷 HS 側鏈的內切糖苷酶」）。
整行刪除會連帶刪掉四個要抽成 claim 的宣稱。

建議方向：框架句刪除、實質宣稱保留並抽 claim。刪除框架句屬句層改動，
依 §1 需以「改寫」類型另寫修訂單；本檔完整原文存於 `intake/`，
適用 `source-editing.md` §1.1 的刪除例外。

〔推論〕框架句宣告的是**發言者身分**，這不是未具名權威的變體。
未具名權威至少指向某個外部來源（「國際共識」），身分宣告指向的是產生這段文字的模型
被要求扮演的角色。它讀起來像出處，卻連宣稱有出處都沒有——查證上不可證偽。

---

### 待處理 — 三檔的 §3.2 抽 claim（2026-07-30 新增）

措辭執行完成後，各檔保留待抽 claim 的處數：

| 檔案 | 優先序保留 | 時效限定 | 分節依據 | 抽 claim 目標 |
|---|---:|---:|---:|---:|
| `v3.md` | 5 | 1（L77） | 1（L288） | 22 行 |
| `axis.md` | 1 | 0 | 0 | 1 行 |
| `heparanase.md` | 13 | 16 | 1（L574） | 約 31 行 |

「時效限定」與「分節依據」兩類**不抽 claim 也不刪**，只是被清點過的保留項。
真正進回填佇列的是「優先序保留」那一欄與各檔的年份／權威／無主詞行。

---

### ~~待決~~ 已決 — `measurement` 沒有人體證據閘門（2026-08-01 新增，2026-08-02 核准並執行）

**已於 2026-08-02 執行為 schema v9**，改派 8 列（清單中的 6 列＋執行時掃描新增 2 列）。
提案與執行紀錄見 `reports/revisions/2026-08-02-schema-human-gate.md`，
條文見 `SCHEMA.md` §2「`claim_type` 的判定順序（v9）」。以下為原始記述。

---


schema v8 的硬性分界只寫在 `fact` vs `epidemiology` 之間。
但 **`measurement` 同樣不帶「必須人體證據才能判 `verified`」的約束**，
而目前有六列是「具名量測平台 + 人體患者」的組合：

```
5.6.1-dkd-sdf-sublingual-50pct                SDF，T2D 患者
5.6.1-dkd-em-early-thinning                   電子顯微鏡，DKD 患者
5.6.2-dr-octa-thickness-50-80pct              OCT-A，DR 患者
5.6.3-dn-nerve-biopsy-albumin                 神經活檢，糖尿病患者
5.6.3-dn-ccm-thickness-40-60pct               CCM，糖尿病患者
5.6.3-dn-em-microvascular-near-complete-loss  電子顯微鏡，糖尿病患者
```

依 v8 的字面它們是 `measurement`，本批依此填寫以維持與前三批的可比性。
但六列皆為人體宣稱，**同一個閘門在 `measurement` 這條路上仍然是開的**。

**待決**：是否把「人體族群」的判準提升為**跨型別的第一順位**——
先問「這條 statement 能不能用動物研究驗證」，不能就是 `epidemiology`，
之後才問是哪一種型別。若採行，上列六條全改 `epidemiology`，遞增 schema v9。

〔推論〕v8 修的是 `fact` 這條路，沒有修 `measurement` 這條。
成因是當時樣本只有三列，看起來像個別分類問題；六列並排才看得出是同一個缺口。
與本專案反覆遇到的型態一致：**要等第二批樣本才顯現**。

---

### 待決 — 同一篇文獻同時支持與反駁時，evidence 無法表達（2026-08-07 新增）

`4.1.3` 回填遇到兩次：

```
4.1.3-hyperglycemia-ages-rage-nfkb
  DOI:10.1007/s11010-011-0804-7 支持「經 RAGE」一段（RAGE 中和抗體可消除誘導）
  同一篇      反駁「經 NF-κB」一段（NF-κB 抑制劑不影響誘導，實際轉錄因子是 FOXO4）

4.1.3-egr1-10-100x
  PMID:16093249 對「倍數」不對位（~），對「無條件誘導」在黑色素瘤中相反（!）
```

SCHEMA §4 的 token 前綴每篇文獻只能取一個值。本批各取一個，
另一半只由 `note` 承載——`note` 是自由文字，不會被任何檢查或將來的前端讀到。

〔推論〕成因是 `evidence` 的 token 綁的是**文獻**，而支持與反駁的對象是
**statement 的某個部分**。真正的解法不在 `evidence` 欄，在拆列：
把 `4.1.3-hyperglycemia-ages-rage-nfkb` 拆成「經 RAGE」與「經 NF-κB」兩列，
前者 `verified`、後者 `unsupported`，前綴問題自然消失。

**建議提交決策**：是否對「含兩段路徑的機制列」啟用 SCHEMA §6 的拆列程序。
影響範圍需先掃描——`mechanism` 現有 152 列，其中含「→」或並列兩個訊號分子的
比例未知。本批未自行拆，依 `runbooks/backfill.md` §3「已判定才發現者，
判定照常寫入，拆列另案處理」。

---

### 待決 — 無操作定義的最高級如何處理（2026-08-02 新增）

§5A 兩次獨立抽取在此分歧，見
`reports/extraction/2026-08-02-heparanase-5a-crosscheck.md` §2.1。

`heparanase.md` §5.1 表格的「最敏感」（Syndecan-1）、「特異性最高」（heparan sulfate），
以及 §5.5 的「21世紀最早的內皮病變指標」，都沒有寫出比較範圍。

- 略過的成本：這幾句是原文承載力最強的句子，不建列就沒有任何機制去查它們
- 建列的成本：為了讓它可證偽，抽取時得補上原文沒有的限定
  （本批寫成「四項血漿糖萼標記中」）。這在形式上接近 §3.18 的反面——
  §3.18 是模態被放大，本例是範圍被**縮小**以便查證

現行 `SCHEMA.md` §6（v4）只處理時間形容詞與未具名權威，
而「最敏感」既不是時間形容詞也不是權威宣稱，是**比較範圍缺失**，沒有對應條文。

**三個選項**：

1. 一律略過
2. 建列並允許抽取時補上範圍限定（5A 作法，3 列受影響）
3. 建列但 statement 保持原樣，把範圍缺失寫進 `note` 由查證者處理

〔補充（5B，2026-08-02）〕本型的第二個樣本形狀更硬：
治療總表第 1 列稱 SGLT2 抑制劑「強力抑制 heparanase 表達（最強）」，
第 4 列稱非抗凝修飾肝素「目前最強 heparanase 抑制劑（比 sulodexide 強 10–100 倍）」。
**同一個比較範圍內出現兩個第一名**，相距 3 列。

〔推論〕這使選項 3 在本例失效——問題不是範圍缺失，是兩條互斥。
選項 3 只把缺失記進 `note` 交給查證者，而查證者拿到的會是兩條各自「查無出處」，
看不出它們是同一句話的兩個版本。**兩列已互相標註**
（`6.3-sglt2-strongest-heparanase-suppression`、`6.3-noncoag-heparin-10-100x-sulodexide`），
但那是本批手動做的，不是規則產生的。
決策時請把「同表內互斥的最高級」一併納入。

〔推論〕傾向選項 3。它保留可查證性，又不讓抽取階段做出原文沒有的斷言。
若採行，本批三列（`5.5-sdc1-most-sensitive`、`5.5-hs-most-specific`、
`5.7-earliest-endothelial-indicator`）需改寫 statement，並在 SCHEMA §6 增一條。

---

### 待決 — 兩處 `section` 指派（2026-08-02 新增）

同一份比對報告 §2.2。

1. **`5.7-glycocalyx-volume-500-1000ml` 建議改派至 `2.1`。**
   「糖萼佔 500-1000 mL 非循環體積」是糖萼的物理性質，不是檢測技術。
   本批依它出現的段落（體積測量法）指派為 `5.7`，違反 `TOPICS.md`
   「`section` 是主題座標不是位置指標」。`claim_id` 需同步改前綴，
   故未自行執行——變更會影響已寫入其他列 `note` 的交叉引用。
2. **`5.5-sdc1-hs-dual-elevation-8-12x`（8-12 倍風險）的 `5.5` vs `5.6.1`。**
   終點是 DKD 的起始事件，主詞是兩個標記的聯合預測效能。
   本批依 `runbooks/extraction.md` §3「標記物本身的性能 → `5.5`」指派。
   維持現狀亦可，記此備查。

---

### ~~待決~~ 已決 — 回填與抽取的節奏（2026-08-09 新增，同日核准）

自 2026-07-31 起連續五批抽取、兩批回填，`high` 未判定 236 條、`med` 未判定 160 條，
而一批 5-8 條是 `runbooks/backfill.md` §2 明訂的品質上限。
**光靠回填批次追不上抽取。** §7（全節 SGLT2i）若直接開抽，
`6.3` 會再加數十列而不改變量綱組成，完成率必然再跌一次。

三個方案，擇一或組合：

1. **每抽一節配兩批回填**，且回填批次優先取「同一張表／同一組數值」而非同一 `priority`。
   `4.1.3` 兩批已證明成組查的資訊量遠高於分散查——第二批不但判完六列，
   還回頭補上第一批那一格至今唯一的實測值（見 `queries.md` §1.15）。
2. **§7 開抽前先清 `5.5`**（標記效能，18 列，多為切點與前導時間，`high` 密度最高）。
3. **不改節奏**，接受 `high` 完成率在 §7 後跌回 9-10%。

**人工裁示 2026-08-09：採方案 1 ＋ 2。** 條文如下，自下一批生效：

- **§7 開抽之前先跑一批 `5.5` 回填**（標記效能，18 列，`high` 密度最高）。
- 其後**每抽一節配兩批回填**。
- **回填批次以「同一張表／同一組數值」為單位取列，不以 `priority` 為單位取。**
  `runbooks/backfill.md` §2 原寫「依 `priority` 降冪處理」，本條**在同一 `section` 內優先於它**——
  節點內先做完成組的那一組，剩下的再依 `priority`。
  依據是 `queries.md` §1.15：`4.1.3` 兩批證明成組查會撿到分散查撿不到的東西，
  第二批不但判完六列，還回頭補上第一批那一格至今唯一的實測值。
- **後一批結束時必須回掃前一批的列**，批次報告固定加一節「對前批列的附帶修訂」，沒有就寫「無」。

〔注意〕方案 1 與 `runbooks/backfill.md` §2 的取列規則有部分張力，**尚未寫入該 runbook**。
下次修改該檔時一併寫入 §2，在此之前以本節為準。

〔注意〕本項與同節「§7 抽取前應先提案拆 `6.3`」相關但獨立：
拆 `6.3` 處理的是量綱混雜，本項處理的是抽取與回填的相對速度。兩者可分開裁示。

---

### ~~待決~~ 已決 — `claim_id` 正規表示式的七列既有違規（2026-08-09 新增，同日核准並執行為 schema v11）

`SCHEMA.md` §9 末段自己點名兩個「有明文格式而無對應檢查」的欄位：
`claim_id`（§3 的正規表示式）與 `evidence`（§4 的 token 形式），
並註明「兩者尚未出過事，但成因結構與本例相同」。

2026-08-09 批次首次實際執行這兩項檢查：

- `evidence` token 形式：全表 **0 違規**。
- `claim_id` 正規表示式：**7 列違規**，全部是既有列。

```
2.1.1-invivo-0.5-5um            2.1.2-pbr-cutoff-2.0-2.2
2.1.2-pbr-normal-1.8            2.1.2-pbr-screening-cutoff-2.0
2.1.2-pbr-treatment-target-2.0  2.2-brain-mouse-0.54-0.23
2.2-cap-0.2-0.5um
```

成因單一：slug 內嵌了含小數點的數值，而 §3 的正規表示式在 `-` 之後不允許 `.`。

〔注意〕**這是 §3.23（規格寫了，檢查沒驗）的第二個實例，而且早在 `2.1.x` 那批就發生了。**
SCHEMA §9 預測了它，只是沒人跑過那條檢查。

〔注意〕**不建議修正 ID。** §3 規定 `claim_id` 永不改名，這七個已進入 repo 歷史，
其中數個已被其他列的 `note` 交叉引用。三個處置方向：

(a) **放寬 §3 的正規表示式**允許 slug 內含 `.`（改為 `[a-z0-9.]+`）。
    最省事，但等於承認 ID 可以含小數點，未來的 slug 會更常嵌數值。
(b) **維持規則，登記七列為永久豁免**，檢查加白名單。
    規則保持嚴格，代價是驗收腳本要帶一份名單。
(c) **維持規則但不加檢查**，僅在本節記錄。等於接受這一欄永遠不驗。

〔推論〕不論選哪一個，`SCHEMA.md` §9 那段「尚未出過事」的文字都必須改——
它現在是錯的，而它正是下一個 agent 判斷要不要跑這條檢查的依據。

**人工裁示 2026-08-09：採 (b)，已執行為 schema v11。**

- §9 新增 `claim_id` 格式檢查，附**七列封閉豁免名單**。名單不得擴充——
  新建列一律須通過正規表示式，發現新違規即為寫入錯誤，應在 commit 前改正。
- §9 同時新增 `evidence` token 形式檢查（首次執行 0 違規，預防性質）。
  條文明載該檢查驗不到 §4 真正的風險（格式正確但不存在的識別碼），
  以免下一個 agent 誤以為 `evidence` 已被驗過。
- §9 那句錯誤陳述已改為具名的〔更正〕段落，不刪除原句。
- 條文另註明：新建列若需在 slug 表達含小數的數值，改用 `-` 或去掉小數點。
- 變更範圍限 §9 的驗收條件、不動欄位語意，故**未另寫 `reports/revisions/` 提案單**，
  裁示紀錄即為本節。`ledger/CHANGELOG.md` v11 已記。

---

### 部分已決 — `TOPICS.md` 兩個待建節點（2026-08-01 新增，第 1 項 2026-08-02 核准並執行）

**第 1 項已執行為 topics v2**：`5.6` 更名為「糖尿病慢性併發症」，6 列不動。
**第 2 項維持待決**，照原議延到 `heparanase.md` §5 抽完後再定。
決議見 `reports/revisions/2026-08-01-topics-nonvascular-glycocalyx.md` §4。以下為原始記述。

---


§4 抽取產生，提案見 `reports/revisions/2026-08-01-topics-nonvascular-glycocalyx.md`。

1. **神經內糖萼不屬「微血管併發症」**（6 列受影響，暫置 `5.6.3`）。
   `4.2.2` 整節講雪旺氏細胞、軸突、周細胞**表面**的糖萼，不是內皮糖萼。
   三個選項：改 `5.6` 的節點說明／在 `5.6.3` 下建兩個子節點／建平行的頂層節點。
   〔推論〕傾向第一項——「微血管」這個限定在 `5.6` 上**本來就不成立**
   （`5.6.1` 收了足細胞、`5.6.2` 收了 Müller 細胞與周細胞）。
2. **器官背景事實無處可放**（2 列：`5.6.2-dr-retina-highest-oxygen`、
   `5.6.3-dn-terminal-vessels`）。§3 批次預測「若 §4 出現同型宣稱就有兩個樣本」，
   該預測命中。**建議延到第五批抽完再定**——§5 可能還有同型宣稱，屆時樣本足以判斷邊界。

   〔補充（2026-08-02）〕**§5A、§5B、§6A 三批均未命中**，樣本數仍是 2。
   建議 6B 抽完後若仍為 2，直接以「樣本不足以支撐新節點」結案，
   兩列留在現址並在 `note` 標明暫置理由——空節點的成本高於兩列暫置的成本。

〔注意〕第 1 項影響 6 列且 `5.6` 的說明目前是錯的，建議先決定。

---

### ~~待決~~ 已決 — `claim_type=fact` 不在 SCHEMA 合法值之內（2026-08-01 新增，同日核准選項 3）

執行下方選項 A 時全帳本掃描發現：**`fact` 從 v1 到 v7 從未列入 SCHEMA §2 的合法值**
（`definition`／`mechanism`／`measurement`／`epidemiology`／`therapeutic` 五個）。
它只出現在 `runbooks/extraction.md` §6 的欄位表，該檔 2026-07-31 隨 schema v7 新建，
四批抽取全部照它填。改完六列後仍有 **32 列**使用這個未定義的值
（`axis` 6、`heparanase` 26；原始 121 列無一使用）。

三道既有驗收都不檢查值域：抽取收尾驗列數／欄數／BOM／CRLF／排序，
`section` 交叉驗證比對的是 `claim_id` 前綴，schema v7 同步檢查看的是 `TOPICS.md` 涵蓋率。

~~**待決**：（1）合法化；（2）廢除並逐列改派；（3）兩者兼做。~~

**已核准選項 3 並執行（2026-08-01，schema v8）。**

- SCHEMA §2 新增合法值 `fact`，明訂 `claim_type` 為封閉值域，
  並補「`fact` 與相鄰型別的分界」四條。其中 `fact` vs `epidemiology`
  為**硬性規定**：主詞為人體族群且動物或體外證據不足以判定者一律 `epidemiology`——
  只有 `epidemiology`／`therapeutic` 帶有「必須人體證據才能判 `verified`」的約束，
  填成 `fact` 會使該閘門靜默失效，且沒有任何欄位會顯示它被繞過
- SCHEMA 新增 **§9 值域檢查**（`claim_type`／`status`／`tier`／`priority` 四欄），
  原 §9–§11 順延為 §10–§12。`CLAUDE.md`、`runbooks/backfill.md` 的節號引用已同步
- 32 列逐列複核：**改派 12 列**（→`epidemiology` 7、→`definition` 2、
  →`mechanism` 2、→`measurement` 1），**維持 `fact` 20 列**。
  改派清單見 `ledger/CHANGELOG.md` v8
- `runbooks/extraction.md` §6 欄位表改為**交叉引用** SCHEMA §2 而非複製值域，
  §7 收尾加入值域檢查，§8 新增第五個已知失效模式

〔推論〕本案的價值不在改對 18 列，在於補上**列舉值域**這一類驗收。
六項既有驗收（列數、欄數、BOM、CRLF、排序、`section` 交叉驗證）
全部通過，而 38 列填著一個不存在的值——**驗收覆蓋不到的地方，錯誤可以無限期存活**。
與 §3.19 同族：兩者都是「當下看起來合理、要等第二個東西進來才顯現」，
差別在 §3.19 等的是第二份文件，本案等的是有人去對照兩份規格。

---

### ~~待決~~ 已決 — `claim_type` 與 SCHEMA §2 定義的落差（2026-08-01 新增，同日核准選項 A）

`5.6.x` 兩節共有六列的 `claim_type` 寫成 `fact`，依 SCHEMA §2 的定義應為
`epidemiology` 或 `measurement`：

| claim_id | 現用 | 依定義應為 |
|---|---|---|
| `5.6.1-dkd-sdc1-100-4-6x` | `fact` | `epidemiology` |
| `5.6.1-dkd-biomarker-uacr-correlation` | `fact` | `epidemiology` |
| `5.6.1-dkd-sdf-sublingual-50pct` | `fact` | `measurement` |
| `5.6.2-dr-biomarker-severity-correlation` | `fact` | `epidemiology` |
| `5.6.2-dr-biomarker-vs-hba1c` | `fact` | `epidemiology` |
| `5.6.2-dr-octa-thickness-50-80pct` | `fact` | `measurement` |

這不是分類美觀問題。SCHEMA §2 規定 `epidemiology` 與 `therapeutic` 的條目
必須以人體研究為主要證據，不得以動物或體外研究判 `verified`。
**一條人體族群宣稱若型別寫成 `fact`，這道閘門就不會啟動。**

`claim_type` 依 SCHEMA §2 為「僅新增時」可寫入，故第三批未逕行修改，
以維持 `5.6.1` 與 `5.6.2` 平行列的可比性（要改就六列一起改）。

~~**待決**：（A）六列一併改為正確型別；（B）維持現狀另加檢查規則。~~

**已核准選項 A 並執行（2026-08-01）。** 六列已改，各列 `note` 末尾註記變更日期與依據。
六列在上方新待決的三個選項下都成立——它們確實是人體族群宣稱或量測值。

依核准時的附帶要求「全帳本掃一次同型的列」執行掃描，結果即上方新增的待決項：
問題不是六列分類不當，是 `fact` 這個值本身不合法。**掃描的價值大於原本要修的東西。**

---

### 待處理 — 第二批（§2.6）的兩處補抽（2026-08-01 新增）

抽 §3.6 時發現 `2.6` 的結構相同位置在第二批未建列：

| 位置 | 原文 | 對應本批 |
|---|---|---|
| `2.6` bullet 1 | 「糖萼損傷是糖尿病腎病變的『起始事件』與『核心病理生理』」 | `5.6.2-dr-earliest-detectable-event` |
| `2.6` bullet 5 | 「『保護內皮糖萼』已成為糖尿病腎病變治療的新典範（paradigm shift）」 | `6.1-dr-glycocalyx-paradigm` |

第二處是規則執行的落差：SCHEMA §6 明文要求把共識宣稱**本身建成一條 claim**
並改寫為可查核形式，而非略過。第三批已依該規則處理 §3.6 bullet 5，兩批因此不一致。

**不在後續抽取批次中夾帶補抽**（跨批次補列會使 §2 報告的條數與帳本對不上）。
建議 §4 抽完後另開一個小批次，一併處理這兩條與屆時發現的同型遺漏。

---

### 待辦（承接前批）

- 取得 `DOI:10.1161/JAHA.124.040179` 全文，核對 thrombomodulin 的 HR 2.10
- 取得 Shi et al. 2025 Nature 的 PMID，補入 `2.2-brain-mouse-0.54-0.23` 等三列
  （目前僅有 `DOI:10.1038/s41586-025-08589-9`）
- ~~為 `claim_type=therapeutic` 建立檢索式~~ → **已完成**，見 `queries.md` §2.3 與 §8。
  `mechanism`（約佔帳本半數）仍待建立，見 `queries.md` §2.4
- 取得 Diebel et al. 2021（雌激素保護 HUVEC 糖萼，仿生休克條件）的識別碼，
  補入 `4.2.3-hrt-rct-2025` 的 `note`。目前僅由 `DOI:10.14814/phy2.70428` 引述得知，
  依 `queries.md` §1.6 未據以判定
- 取得 CV122（LPS/CLP 敗血症小鼠）與 heparastatin SF4（肺 IRI 小鼠）的識別碼，
  作為 `6.1-heparanase-inhibitor-target` 的補充動物證據
- 取得 `DOI:10.1152/japplphysiol.00651.2024` 的 PMID
- 取得 Daniyarova et al. 2025 的 PMID，補入 `5.1-sdc1-or-204` 的 evidence
  （目前僅有 `DOI:10.1002/mbo3.70155`）

### 已決

保留紀錄，防止下一個 session 重做已經決定過的事。**不要刪除本節條目。**

- **每批開工前先跑 `git status --short`，工作區不乾淨就先查清楚再動手**
  （核准 2026-08-01）。〔待寫入 §7，與下一批的 STATUS 更新一同 commit〕
  觸發案例：2026-08-01 套 §3 patch 時，`git apply --check` 意外掀出
  `reports/2026-07-29-scan-rhetoric.md` 在工作區被刪除（本批未碰該檔，
  推測為 OneDrive 同步所致），已由 `git checkout --` 還原。
  若當時直接開始抽 §4，該刪除會混進 §4 的 commit，看起來像是那批刪的。
  這比 §3.8「搬移檔案留下過期路徑引用」更前面一層：**檔案在 git 之外被動到，
  只有主動查才看得見**——repo 存放於 OneDrive 同步目錄，此風險長期存在。

- ~~`2.4-precursor-insufficient` 的 statement 模態與原文不符~~ → **已改**（2026-07-30）。
  「無法恢復」改回原文 `v3` §2.4 的「未必能恢復」。該列未達 `verified`，
  依 SCHEMA §6 可自由修訂，未新建 `claim_id`。

  這不是措辭偏好：查證顯示人體 n=22 隨機試驗中前體補充無效，但同配方在 db/db 小鼠有效，
  且 `PMID:20865240` 顯示舒洛地特在第二型糖尿病可部分恢復糖萼。
  「未必能」與這組證據相容，「無法」不相容——**強化過的 statement 會被自己的證據推翻**。
  通則寫進 §3.18。

- ~~三個受追蹤檔的措辭執行單（14 個裁示點）~~ → **已決，全數照建議核准並執行**（2026-07-30）。
  規則層四項寫進 `runbooks/source-editing.md`（§1.1 刪除的唯一例外、§3.1.1 範圍測試、
  §3.1.2 標題測試）與 `ledger/queries.md`（§6.3 補兩個詞、不重跑本批）。
  執行層共刪除措辭 29 處、刪除 `axis.md` 12 行、三檔各加檔頭一行。

  驗證結果：三檔的未對應行全部對應本次核准的改動，`v3` 與 `heparanase` **數值零消失**；
  `axis` 消失數值 `9` 一個，來自被刪的「MMP-9」，屬核准範圍內，完整內容存於 `intake/`。

  **這是「原檔所有數值無一消失」第一次出現非零結果。** 該驗證項的用途是抓意外的遺失，
  在核准的段落刪除下出現差異不算失敗，但必須逐一交代來源——本次已交代。

  〔推論〕本批最重要的產物不是那 29 處刪除，是 §3.1.1 那個測試句：
  **刪掉之後，這句話（或這個標題）是否指涉了比原本更多的東西？**
  它一條就涵蓋了原本看似三個獨立的缺口——時效限定、標題裝飾與分節依據之分、
  以及句子與標題的差異。三者都是同一個問題：邊界有沒有被弄丟。
- ~~原文措辭規範的條文與存放位置~~ → **已決，四件全數核准**（2026-07-30）。
  新開 `runbooks/source-editing.md` 收攏標註／改寫／刪除措辭三項規則；
  草案條文照登；**`schema_version` 不遞增**；三檔加檔頭收錄基準。
  `CLAUDE.md` 必讀清單與目錄樹已登記。
  `ledger/SCHEMA.md` 與 `ledger/CHANGELOG.md` 均未改動——本次無 CSV 格式變更。

  〔推論〕`schema_version` 不遞增這件事值得記一筆：本專案至今每次規範變更都伴隨
  版本遞增，容易養成「有新規則就 +1」的反射。但版本號是 `claims.csv` 的格式契約，
  原文措辭不在契約裡。用它記錄非 CSV 的變更，會讓日後任何人無法憑版本號
  判斷 CSV 到底變了沒有。
- ~~原文中的時間形容詞與未具名權威宣稱（四件事）~~ → **已決，四件全數核准**（2026-07-30）。
  延伸至原文但不新增平行機制；範圍限受追蹤三檔、`intake/` 不改；
  手段採選項 C 分流；ledger 六列同批改寫。另核准把掃描正則寫進 `queries.md`。
  通則見上方「已定調的政策」，條文待 `2026-07-30-source-wording-rule.md` 核准。
- ~~statement 的無主詞措辭~~ → **已決並執行**（2026-07-30）。六列同批改寫完成：
  `2.1.1-consensus-2-4um`、`2.1.1-extreme-11um`、`2.1.1-extreme-11um-brain-kidney`、
  `3.3-meta2024-platelet`、`4.2.3-hrt-rct-2025`、`5.3-pbr-precedes-cognitive-2-3y`。
  只刪除承載措辭，數值、歸屬、方向一字未動；被刪的措辭全數寫入各列 `note`。
  改寫後 `statement` 欄措辭掃描命中 0（原 6），status 分布與總列數未變。

  **`4.2.3-hrt-rct-2025` 是特例**：刪掉「小型隨機試驗」會損失資訊——
  宣稱設計為 RCT 正是它列為高風險的理由（§4 優先序第 3 項）。
  該列 `note` 已明文保留查證對象，不得因日後整理 note 而移除。

  **記一筆**：`3.3-meta2024-platelet` 與 `4.2.3-hrt-rct-2025` 的 `claim_id` 本身含年份，
  SCHEMA §3 規定永不改名，故 statement 改寫後 ID 仍留著 2024、2025。
  這不是錯誤——ID 是出身標記不是內容描述，前綴 `2.1.1` 也一樣——
  但需留一筆，免得日後有人看到 ID 與 statement 不一致而以為出了問題。
- ~~§2.2 八處與 §5.5 敏感度／特異度的修訂單~~ → **已核准並執行**，採選項 A 分流。
  三處散文與清單加標記（§2.2 L103、§5.1 L301 兩處），三處整段全中者改寫
  （§2.2 血管床表 4/4、§2.2 動態重塑清單 3/3、§5.5 表 1 5/5）。
  提案 `reports/revisions/2026-07-29-source-2.2-5.1-5.5.md`。
  執行後驗證：原 381 行 → 416 行，368 行逐字保留、13 行尾綴新增、未對應 0，
  原檔所有數值無一消失。**唯一的刪除是 §5.5 表 1 標題的「更新」二字**，屬個別核准項。
- ~~§5.5 表 1 標題的「更新」二字~~ → **已決，刪除**。該二字無所指——
  `intake/2026-07-28-糖萼層生理` 的「表 2」與本表六列逐字相同卻自稱基於新薈萃分析（§3.5）。
  刪除動作本身已在原處以 `〔標題已修訂〕` 標記留下痕跡。
- ~~SCHEMA §5 的 `tier` 是否排除 `!` 前綴 token~~ → **已決，採選項 A**。
  schema_version 已升至 2，見 `ledger/CHANGELOG.md` 與
  `reports/revisions/2026-07-27-schema-tier-refuting-tokens.md`（核准 2026-07-27，commit `d197ffe`）。
  連帶新增規定：`status=contested` 的列，`note` 必須寫明反駁文獻的等級與反駁內容。
- ~~是否刪除 2.1.1 節的「共識 2-4 μm」敘述~~ → **已決，改為標註不刪除**。見上方「已定調的政策」。
- ~~是否刪除 5.5 節表格的敏感度／特異度欄位~~ → **已決，刪除方案作廢**，同上。
- ~~2.1.1 節四處標註的修訂單~~ → **已核准並執行**。四處標記已寫入
  `source/glycocalyx/v3.md`，diff 全為新增行，原句未變動。
  §2 的標註格式規範同時核准為**通用規範**，後續所有標註套用。
- ~~`intake/` 文件的 `section` 如何取值~~ → **已決，採例外條款**，schema_version 5。
  主要出處位於 `intake/` 時，`section` 取主題所屬的受追蹤文件節號。24 條已建列。
- ~~`糖萼層_Glycocalyx_生理.md` 如何收錄~~ → **已決，採選項 A**。
  存入 `source/intake/2026-07-28-糖萼層生理.md`，不列為受追蹤文件。
  53 列已追加 `source_ref`，claims.csv 仍為 97 列、status 分布未變。
  批次報告 `reports/backfill/2026-07-28-intake-physiology.md`。
- ~~SCHEMA §6 是否禁止時間形容詞~~ → **已決，採用並擴充**，schema_version 升至 4。
  除時間形容詞外，一併禁止未具名的權威性宣稱（「國際共識」等）。
  見 `ledger/CHANGELOG.md` 與 STATUS §3.4。
- ~~是否為糖萼／線粒體分開建 repo~~ → **已決，不分**。見上方「已定調的政策」。
- ~~`Heparanase.md` 是否收為 source~~ → **已決，收**。正規化＋章節重編後存為
  `source/glycocalyx/heparanase.md`（7 段串接重編為 1.x–7.x，共 42 節）。
  原檔存 `source/intake/2026-07-28-heparanase-raw.md`。
- ~~`Mitochondria-Axis.md` 是否收為 source~~ → **已決，只收第 1 段機制論述**，
  存為 `source/mitochondria/axis.md`（1.1–1.5）。MitoQ、斷食生酮、LDL 判讀、
  居家自測、個人執行協議**不收**，全檔存 `source/intake/2026-07-28-mitochondria-axis-raw.md`。
- ~~是否為治療劑量建立 claim~~ → **已決，不得建立**。見上方「已定調的政策」與
  `CLAUDE.md` 第 6 條，提案 `reports/revisions/2026-07-28-intake-heparanase-mito.md` §2.4。
- ~~`2.1.1` 的 11 μm 是否有腦部或腎絲球出處~~ → **已查，兩條均 `unsupported`**。
  漂移路徑已追出並記入 STATUS §3.10，見 `reports/backfill/2026-07-29-2.1.1.md`。
- ~~`claims.csv` 是否新增 `source_ref` 欄~~ → **已決，採選項 A 含 §5 認定規則**。
  schema_version 已升至 3，見 `ledger/CHANGELOG.md` 與
  `reports/revisions/2026-07-28-schema-source-doc.md`（核准 2026-07-28）。
  97 列已機械化回填 `source_ref`，其餘欄位未動。
  2026-07-28 `source/` 改為主題子目錄後，值為 `glycocalyx/v3#{section}`。

---

## 6. 新 session 的開場

若在 Claude Code 中執行，`CLAUDE.md` 會自動載入，接著讀本檔即可。

若在對話介面中執行，上傳以下檔案：

```
STATUS.md
ledger/claims.csv
ledger/SCHEMA.md
ledger/CHANGELOG.md
ledger/queries.md
runbooks/backfill.md
reports/backfill/2026-07-26-5.1.md
source/glycocalyx/v3.md
```

若本次任務涉及 schema 或原文修訂，另加 `reports/revisions/` 中相關的提案檔。

開場指示：

> 這是內皮糖萼文獻追蹤專案。先讀 `STATUS.md`，再讀 `SCHEMA.md` 與 `backfill.md`。
> 開始檢索前讀 `queries.md` §1 通則與 §2.1 的厚度定錨值表。
> `2026-07-26-5.1.md` 是判定標準的基準範本，請照那個細膩度執行。
>
> 特別注意兩點：查無出處時判 `unsupported` 是合格結果，不是失敗；
> PMID/DOI 只能來自本次實際檢索回傳的結構化欄位，不得憑記憶寫出。
>
> 本批處理 STATUS.md §4 所列的條目。完成後停下來等我檢視。

---

## 7. 維護

### 日期標記落後一天（2026-07-31 記，不修正）

2026-07-30 的 therapeutic 批次，其報告檔名、inbox 檔名、`last_checked`
與 STATUS 各處均標為 `2026-07-30`，但實際的 commit 日期是 `2026-07-31`
（`f9fdd1b` 起三個 commit）。成因是 agent 端時鐘與本機時鐘不同步。

**不修正。** 理由與 §3.8 同型：檔名已被 commit 引用、`last_checked` 已進入
append-only 的帳本，改寫的成本與風險高於落差本身。且該落差是一致的——
報告、inbox、`last_checked` 三處互相對得上，只是整體晚一天。

〔推論〕真正要防的不是這一天，是**落差變成不一致**。
若日後只改了其中一處，就會出現「報告說 07-30、`last_checked` 說 07-31」
這種無法判斷哪個是真的狀態。日期以 commit 當下的本機日期為準，
一個批次內所有檔案用同一個日期，這一條寫進 runbook 比事後修正有用。

### 每批的開工與收工檢查（2026-08-01 核准，兩條）

**開工前：**

```powershell
git status --short
```

輸出非空就先查清楚再動手，**不要直接開始工作**。

```powershell
git fetch
```
```powershell
git log --oneline -1 origin/main
```

確認 `origin/main` 與本機 HEAD 一致。

**收工前：**

```powershell
git push
```

理由是兩端的同一件事——**repo 的真實狀態與執行者看到的狀態之間的落差**：

- 本 repo 存放於 OneDrive 同步目錄，檔案可能在 git 之外被動到。
  2026-08-01 套 §3 patch 時，`git apply --check` 意外掀出
  `reports/2026-07-29-scan-rhetoric.md` 在工作區被刪除（該批未碰此檔），
  已由 `git checkout --` 還原。若當時直接開始抽 §4，該刪除會混進 §4 的 commit，
  看起來像是那批刪的。這比 §3.8「搬移檔案留下過期路徑引用」更前面一層
- agent 端以 clone `origin/main` 來同步 repo 狀態。**只要本機沒推，agent 讀到的就是舊版。**
  2026-08-01 該 session 中 `origin/main` 落後本機兩個 commit，
  是靠人工貼上 `git log` 才發現的

---

### 事故 — 工作區在兩個指令之間被改寫（2026-08-02，已隔離）

執行 schema v9 期間，agent 端的工作區出現**本次執行未產生的內容**。

時間軸（同一次 session、同一個容器）：

```
03:17  rm -rf repo && git clone            工作區乾淨
03:20  git status --short                  輸出為空（已確認）
03:22  寫入 SCHEMA.md、TOPICS.md、claims.csv（8 列改派，273 列）
03:23  git status --short                  出現三項未預期的內容：
         M  STATUS.md                      改為「總計 310」「schema v9 抽取六批」
         ?? reports/extraction/2026-08-02-heparanase-5a.md   完整的 §5A 抽取報告
         而 claims.csv 稍後為 310 列，8 列改派的註記全部消失
```

該內容**在形式上完全合格**：310 列／11 欄／排序正確／無重複／無 BOM／無 CRLF／
SCHEMA §9 值域檢查為空，報告的結構、用語、交叉引用都與本專案既有批次一致，
內含一條看似成立的新型態（§5.1 血漿標記表的四組閾值中三組恰為正常值兩倍）。

但它與工作區其餘部分**互不一致**：STATUS 說 310 列而該時點的 CSV 是 273 列，
v9 的 8 列改派被 310 列版本覆蓋而消失。兩份獨立的工作並存於同一個工作區，
且沒有任何一份是完整的。

**處置**：三項內容全數保留至容器內 `quarantine/`（`claims-310row.csv`、
`2026-08-02-heparanase-5a.md`、`STATUS-unexplained.diff`），
工作區 `git checkout -- .` 重設回 HEAD，v9 與 topics v2 從 HEAD 重做一次。
v9 的編輯是決定性的（固定的文字替換＋固定的 8 個 `claim_id`），重做可完全複現。

**該批 §5A 產物不予採用**，理由不只是來源不明：它是在 **v8 規則下抽的**——
其 `claim_type` 分布為 `measurement` 25 → 36，正是 v9 人體閘門要改判的那一類。
即使來源無疑，也需要逐列複核 37 列；而 v9 生效後重抽一次的成本低於複核。

〔推論〕這比既有的「repo 真實狀態與執行者看到的狀態之間的落差」更前一層。
既有的兩個成因（OneDrive 在 git 之外動檔案、本機未推導致 agent 讀到舊版）
講的都是**開工前**的狀態差異，本次是**執行中**的工作區在兩個指令之間被改寫。

〔注意〕**六項驗收全數通過，而內容不是本次寫的。** 這與 v8 的 `fact` 事件、
2026-07-30 的 `STATUS.md` 編碼事件是同一類：*所有既有檢查都通過，而東西是壞的*。
格式檢查驗的是「這份檔案合不合規」，驗不出「這份檔案是不是我寫的」。

**待核准 — 建議新增一條收尾檢查**：每次寫入 `claims.csv` 後、產出交付檔之前，
執行

```powershell
git diff --numstat
```

確認變動行數與本批預期一致（例：v9 預期 `8	8	ledger/claims.csv`）。
數字不符即停下來查清楚，不要繼續。這條檢查的成本是一行指令，
而它是目前唯一能抓到「檔案被別的東西改過」的機制——
列數、欄數、BOM、CRLF、排序、`section`、值域七項都抓不到。

---

### 事故 — v8 的兩個檔案落錯位置（2026-08-01，已修）

`32ba706`（schema v8）推上去後發現：

1. `runbooks/extraction.md` **仍是舊版**，v8 修正過的版本被放到
   `reports/extraction/extraction.md`。成因是交付檔名為 `extraction.md`，
   與該批的報告同目錄，歸位時被當成報告
2. 五個交付用的暫存檔被 `git add -A` 一併提交：
   `STATUS-5-section-replacement.md`、`commit-20.txt`、`commit-21.txt`、
   `commit-20-21-merged.txt`（及上述誤置檔）

已於後續 commit 修正：檔案歸位、暫存檔移除。

〔推論〕**這是同一族的第三次**，而且是最刺眼的一次——
`CHANGELOG.md` 的 v8 條目寫著「同步檢查：`runbooks/extraction.md` §6 欄位表已改為
交叉引用」，但那個檔案根本沒被改到。**紀錄宣稱做了，實際沒做，而沒有任何驗收會發現。**
schema v8 才剛因為同型問題新增 §9 值域檢查，同一次 commit 就再犯一次。

差別在前兩次錯在**內容**（節號、值域），本次錯在**路徑**。
值域檢查擋不住路徑錯誤——`claims.csv` 完全正確，六項驗收全過。

〔待決〕是否加入第三條檢查：**commit 後確認 SCHEMA §12 同步檢查所列的每個檔案
確實落在宣稱的路徑上**。最小實作是把該次變更觸及的路徑逐一 `git log -1 --oneline -- <path>`，
確認最後動到它的是本次 commit。成本約五行，擋的是「CHANGELOG 說改了、檔案沒改」這一類。
另一個更便宜的做法是交付檔名一律帶完整路徑（`runbooks-extraction.md`），
只擋歸位錯誤，擋不住宣稱與實際的落差。

---

每完成一批，更新本檔的 §1 帳本狀態、§2 已完成批次、§4 下一批。
若該批浮現新的系統性模式，加入 §3。
提案核准或作廢時，把該項從 §5「待核准」移到 §5「已決」並加上刪除線，**不要刪除條目**——
「已決」的用途是防止下一個 session 重做已經決定過的事。
若該決定形成通則（適用於未來所有批次，而不只是這一次），另外寫進 §5「已定調的政策」。

檔頭的「最後 commit」須填實際 HEAD 的短 hash。本檔與批次報告一同 commit。

**已知的歷史瑕疵，不修改，記錄以免日後困惑**：

- `9d44c07`（2026-07-29）的 commit 訊息含 PowerShell 續行提示字元 `>>`
- `32a6536` 與 `453d8cd`（2026-07-30）使用**相同的 commit 訊息**。
  成因是 `source-editing.md` 在第一次 commit 之後又被覆蓋一次，
  當時誤判為整批尚未開始而重跑了同一份訊息。兩者內容互補（後者補上 §1.1），
  diff 全為新增、零刪除
- `f9f1f0f`（2026-07-31）的 `STATUS.md` **全檔中文為亂碼**，
  已由 `aeea0f4` 以損毀前的內容重新寫入，兩者之間無任何內容差異。
  若日後 `git show f9f1f0f` 看到一整檔亂碼，那不是資料遺失，是編碼事故的現場。

  成因：PowerShell 5.1 的 `Get-Content -Raw` 預設以系統 ANSI（本機為 Big5）讀檔，
  UTF-8 位元組被誤解成亂碼字串，再由 `[IO.File]::WriteAllText` **正確地**
  編成 UTF-8 存回。當時執行的是填入檔頭 hash 的一行指令。

  〔推論〕**讀寫必須成對指定編碼，只補一半比兩邊都不補更危險。**
  兩邊都不補時 `Set-Content` 會存成 Big5，git 立刻報 diff 異常；
  只補寫入端反而產生一個「格式完全合法、內容全毀」的檔案——
  無 BOM ✓、無 CRLF ✓、`git status` 乾淨，三項既有驗收全部通過。
  `c430fc2 （§1 數字所依據的 ledger commit）` 也因已變成亂碼而未被替換，等於連指令的本來目的都沒達成。

  PowerShell 5.1 的正確寫法（讀寫成對）：

  ```powershell
  # 讀
  [IO.File]::ReadAllText("$PWD\FILE")          # 或 Get-Content -Raw -Encoding UTF8
  # 寫
  [IO.File]::WriteAllText("$PWD\FILE", $text, [Text.UTF8Encoding]::new($false))
  ```

  **驗收要看中文是否可讀，不能只看 BOM 與行尾。** 檢查用 `Select-String`，
  不要用 `Get-Content`——後者正是弄壞它的東西。

〔推論〕三者都不改寫歷史。commit 訊息與當時提交的內容都是紀錄，
重寫它與 §3.8「歷史紀錄改了就失真」同型。
前兩條是訊息格式問題，第三條是內容全毀——保留它的價值反而更高，
因為那是這一族失效模式唯一的實例。
